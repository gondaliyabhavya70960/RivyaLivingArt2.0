// Re-exported from the shared studio helpers so every form uses one copy.
export { FormSection } from "@/components/studio/form-section";
export { FieldError } from "@/components/studio/field-error";
