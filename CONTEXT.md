> **ARCHIVED — superseded, kept for history.** This document does not describe the
> repository as it stands. The live reference is **PROJECT_STATE.md and CLAUDE.md**.
>
> Owner decision D24 (2026-09-03) approved archiving the superseded documents. They
> are bannered in place rather than moved under `docs/archive/`: `DESIGN.md` alone has
> 164 inbound references, and many of them sit in dated records — CHANGELOG entries,
> PROJECT_STATE history, prior audits — where rewriting the path would falsify what
> those documents said at the time. A banner marks the file without editing history.

> **HISTORICAL — v1 build memory, superseded.** This file records the original
> v1 "Midnight Sapphire" build and is no longer the source of truth: **CLAUDE.md**
> (project instructions, "Current repo reality") + **DESIGN.md** (v2.0 spec) are
> authoritative and contradict this file on routes, stack, theme, and status.
> The repo has since been rebuilt as v2.0 "Midnight Gild" (Phase 7 commit
> b1cf3e6; audited through PR #95); the default/integration branch is **Main**
> and PRs merge into it — not the qwf75k build branch named below. Do NOT treat
> §§6–12 (schema/API/component inventories, status, next steps) as current;
> several later entries here already contradict earlier ones (e.g. PERF-006/007,
> MKT-005, S4 blog covers). Re-verify against the working tree before actioning
> anything in this file.

# CONTEXT.md — ResinRiva Build Memory (historical, v1)

> **READ THIS FILE FIRST in every session. Do NOT re-read the whole codebase.**
> Work on ONE phase at a time. When a phase finishes: update this file completely, STOP, and wait for the user's explicit permission before the next phase. Never auto-continue.

---

## 1. COMPLETED PHASES & FEATURES

### ✅ Phase 1 — Repo + Documentation (complete)
- Git repo initialized on branch `claude/resinriva-ecommerce-build-qwf75k` (remote: github.com/gondaliyabhavya70960/ResinRiva2.0 — remote was empty; this branch is the first ever pushed, so it became the repo default and **no PR base exists yet**).
- Files created (README.md first, per spec):
  - `README.md` — project overview, business info, hard rules, tech stack, phase checklist, known caveats (caveats live in README per spec)
  - `CONTEXT.md` — this memory file
  - `INSTALL.md`, `DEPLOYMENT.md`, `ADMIN_GUIDE.md`, `CONTENT_GUIDE.md`, `SEO_GUIDE.md`, `BACKUP_GUIDE.md`, `WHATSAPP_ORDER_GUIDE.md` — documentation stubs with full outlines (content filled as phases build the features)
  - `COMPETITOR.md` — stub with entry template (populated in Phase 11)
  - `.gitignore` (Next.js/Prisma/Vercel standard), `.env.example` (names only)
- No application code yet — scaffold happens in Phase 2.

### ✅ Phase 2 — Scaffold + Blue Design System (complete)
- **Stack scaffolded**: Next.js 16.2.10 (App Router, TS, Turbopack), React 19.2.4, Tailwind CSS v4 (CSS-first `@theme`), ESLint 9. Installed: `motion` 12, `gsap` 3.15 (ScrollTrigger + SplitText — free, registered in `src/lib/gsap.ts`), `lenis` 1.3, `lucide-react` 1.23 (⚠ NO brand icons — use inline SVGs for WhatsApp/Instagram), `react-hook-form` + `zod` 4 + `@hookform/resolvers`, `embla-carousel-react`, `@react-three/fiber` 9 + `drei` 10 + `postprocessing` 3 + `three` 0.185, `@vercel/blob` 2, `@vercel/analytics`, `@vercel/speed-insights`, `class-variance-authority`, `clsx`, `tailwind-merge` 3, Radix primitives (dialog, accordion, label, select, separator, slot, dropdown-menu, checkbox), `tw-animate-css`.
- **Midnight Sapphire dark theme** (public site): the public layout (`src/app/(public)/layout.tsx`) is wrapped in `.dark`, whose token block in globals.css is the dark palette — `--background #0a0a0a` (rich black), `--foreground #f8f9fa` (off-white), `--card #101319`, `--primary` = sapphire, gold-for-CTA (the default `Button` variant is `bg-gold text-void`). `--void` is now `#0a0a0a` and `--porcelain` `#f8f9fa`. The `/studio` admin stays on the light `:root` scheme. Literal light utility classes (`text-midnight`, `bg-porcelain`, `bg-ice`, `bg-white`, `border-midnight`) across `src/**/*.{ts,tsx}` were swept to **adaptive semantic tokens** (`text-foreground`, `bg-background`, `bg-muted`, `bg-card`, `border-foreground`) so they render dark under `.dark` (public) and unchanged under `:root` (studio). Prose classes in `src/lib/tiptap-render.ts` were swept too (legal + blog article bodies). Eyebrow labels use `--azure` for contrast on black.
- **Design tokens** in `src/app/globals.css`: all §4 colors as `:root` vars + `@theme inline` (bg-sapphire, text-porcelain, …), shadcn semantic vars mapped to the blue system, blue-tinted shadows (shadow-luxe-sm/luxe/luxe-lg/glow), custom utilities: `glass`, `glass-dark` (solid 0.92 void glass), `text-display`, `text-display-sm`, `eyebrow`, `section-number`, `text-header-ink(-soft)`, `gradient-dopamine`, `gold-rule`, `font-display`; `animate-mesh-drift`, `animate-shine`; `--ease-luxe`; global reduced-motion kill-switch; base styles wrapped in `@layer base`.
- **Fonts**: Fraunces (`--font-fraunces`, display) + Inter (`--font-inter`, body) via next/font/google, display swap, wired in root layout.
- **Layouts**: root layout (fonts, SmoothScrollProvider, Analytics + SpeedInsights) · `(public)/layout.tsx` (Preloader, DynamicHeader, main, Footer, FloatingWhatsApp — zero top padding, hero starts under fixed header).
- **Components built** (inventory with props in §8): all Phase 2 primitives + DynamicHeader system + Footer + FloatingWhatsApp + 12 shadcn-style ui components authored in-repo.
- **Temporary demo home** at `(public)/page.tsx` — design-system preview with alternating light/dark bands; REPLACED in Phase 6.
- **Verification**: `npm run build`, `tsc --noEmit`, `npm run lint` all clean. Browser suite (Playwright, production build): **17/17 checks** — header ink switching on void/ice/midnight/footer bands, ≤1 ink change across a slow boundary scroll (no flicker), glass surface after 24px, mobile menu solid void-glass with light ink, wa.me link exact, reduced-motion static fallbacks, preloader suppressed under reduced motion.

### ✅ Phase 3 — Database + Auth (complete)
- **DB**: Prisma 7.8 (`prisma-client` generator → `src/generated/prisma`, gitignored; `prisma.config.ts` holds the datasource URL + seed cmd — Prisma 7 does NOT read .env automatically, dotenv imported there). Runtime client via `@prisma/adapter-pg` in `src/lib/db.ts` (works for local Postgres AND Neon pooled URL — zero-change swap when the owner connects Neon in Vercel). Local dev DB: postgres 16 in-container (`postgresql://resinriva:resinriva_dev@localhost:5432/resinriva`, in the gitignored `.env`); migration `20260705183035_init` applied; **the owner must run `prisma migrate deploy` against Neon at deploy time (Phase 10)**.
- **Schema**: ALL §3 models + enums migrated (Role, ContentStatus, FieldType, InquirySource/Status, MediaType, ScrapeTier/Platform/JobStatus/ReviewStatus; Product.needsRewrite, occasions Json, careNotes; ScrapedProduct @@unique([sourceKey, externalId]); cascades on ProductImage/CustomizationField/PortfolioImage/ScrapedProduct; SiteSettings singleton id "main"; ActivityLog.meta Json added).
- **Seed** (`prisma/seed.ts`, idempotent upserts): admin from ADMIN_EMAIL/ADMIN_PASSWORD (bcrypt 12); the 16 real categories (slugs: resin-furniture-surfaces, art-craft-pieces, varmala-preservation, wedding-photo-frames, resin-trays-serving-platters, candle-tea-light-holders, resin-wall-clocks, resin-jewelry-keychains, resin-home-decor, resin-vases, drinkware-barware, tablespace-sets, sculptures-objets, vanity-mirrors, kids-room-decor, workshops) with price-band reference in descriptions; 6 FAQs (delivery, packaging, photo quality, care, price/payment, returns); SiteSettings with exact business values; privacy+terms Page stubs (real content Phase 6); 2 DEMO products (titles start "DEMO — …", slugs demo-*) with CustomizationFields for UI building — **DELETE in Phase 10**.
- **Auth**: Auth.js v5 (next-auth beta.31) JWT sessions; `src/lib/auth.config.ts` = edge-safe half (no db) used by `src/middleware.ts` (protects /studio/*, allows the public auth pages login/signup/forgot-password/reset-password, bounces signed-in users off login+signup, callbackUrl support); `src/lib/auth.ts` = full instance with Credentials provider (zod + bcryptjs against User table); handlers at `app/api/auth/[...nextauth]`; session carries user.id + role (module augmentation).
- **Studio auth pages** (`src/app/studio/{login,signup,forgot-password,reset-password}`): share `src/components/studio/auth-shell.tsx` (void + GradientMesh luxury card, on-brand — NOT the purple/gaming reference), `password-field.tsx` (show/hide), `submit-button.tsx` (useFormStatus pending). Pre-auth server actions live in `src/actions/auth-public.ts` (NOT requireStaff-guarded): `signUpFirstAdmin` (first ADMIN only while user count === 0 — one-time owner bootstrap, closed forever after; no customer accounts), `requestPasswordReset` (neutral response, no enumeration) + `resetPassword` (hashed single-use 1h token). `src/lib/tokens.ts` = sha256 token gen/hash; `src/lib/email.ts` gained `sendPasswordResetEmail` (Resend REST via `RESEND_API_KEY`/`EMAIL_FROM`, logs the link as fallback when unconfigured). User model gained nullable `resetTokenHash` + `resetTokenExpiry` (migration `20260706062409_add_password_reset`).
- **Also**: `src/lib/rate-limit.ts` (sliding-window per-key limiter + passesSpamChecks honeypot/min-fill helper); `/studio/login` (void glass card, error state) + placeholder `/studio` dashboard (replaced in Phase 4).
- **Measurement + funnel a11y (audit Phase 1a)**: `components/analytics/whatsapp-tracker.tsx` — a single delegated click listener mounted in the public layout instruments EVERY `wa.me` anchor site-wide (floating/hero/header/footer/page CTAs) → `track('whatsapp_cta_click',{source,path})` + Meta Pixel `Contact`; attribution via `data-wa-source` (added to floating + footer), else pathname (MKT-001). `components/analytics/marketing-scripts.tsx` — env-gated Meta Pixel + GA4 via `next/script`, OFF unless `NEXT_PUBLIC_META_PIXEL_ID`/`NEXT_PUBLIC_GA_ID` set (MKT-002; CSP already allow-lists connect.facebook.net/googletagmanager). Order + custom-order forms now emit `*_started`(first interaction) / `*_submit_attempt`(pre-async) / `*_submitted`(ok-branch) instead of one post-submit misfire (MKT-007). A11Y: `.dark --destructive #ef7d72` (error text clears AA, A11Y-001) + `.dark --ring #5b9dff` with the global focus outline + button ring using it (A11Y-002).
- **Site Settings → live chrome (audit Phase 1b, ENG-001/MKT-005/MKT-009)**: `lib/site-settings.ts` `getSiteSettings()` (cache()'d, id:'main', per-field fallback to SITE) is loaded once in the async public layout and threaded into `DynamicHeader` (announcement + href + waHref props), `AnnouncementBar` (message/href — now clickable to /shop, MKT-009), `Footer` (phone/email/maps/whatsapp + Instagram only when a real URL is set, MKT-005), and `FloatingWhatsApp` (waHref). `buildWaLink(message?, number?)` takes the studio-configured number. Organization/LocalBusiness JSON-LD `sameAs` now populates from settings socials. So editing the announcement / WhatsApp number / socials in Studio now changes the live header/footer/announcement/floating (was write-only). **Verified**: setting distinct values in the DB flips the announcement text, all chrome wa.me links, footer Instagram, and JSON-LD sameAs. **Follow-up**: page-BODY WhatsApp CTAs (home hero + CTA band + about/faq/process/contact/portfolio/search/workshops) still use the SITE default number — a separate per-page sweep.
- **Newsletter lead capture (audit Phase 1c, MKT-003)**: new `Subscriber` model (email @unique, source, createdAt; migration `20260713065931_add_subscriber`) + `subscribeEmail` public action in `actions/public.ts` (zod email + honeypot + fill-time + per-IP rate limit; idempotent upsert by lowercased email; bots get silent success). `components/sections/newsletter-form.tsx` (client; honeypot + formStartedAt like the other public forms) is mounted in the footer ("Get first access to new drops") and the homepage launching-soon state; fires `newsletter_subscribe`. **Verified** end-to-end: submit → success state + Subscriber row with the right source.
- **Form-error a11y + review schema + CRO copy (audit Phase 1d)**: A11Y-004 — every public form field now pairs `aria-invalid` with `aria-describedby` pointing at an `id`'d error `<p role="alert">` (contact-form: name/phone/email/message; order-panel: notes/name/phone/email + file error). RHF's contact form already focuses the first error; the custom-validated `order-panel` now mirrors that — a `formRef` + `requestAnimationFrame(() => form.querySelector('[aria-invalid="true"]')?.focus())` on failed submit. MKT-006 — homepage emits a `LocalBusiness` `AggregateRating` + `review[]` JSON-LD (same `@id` as the layout's LocalBusiness so engines merge it) computed from the real `Testimonial` rows only; **no testimonials → no schema, never fabricated** (MKT-004 testimonial *content* stays owner-authored). MKT-008 — product page shows 3 trust bullets (hand-poured in-studio · shipped insured across India · price/timeline on WhatsApp, no payment now) between the price chip and order panel. MKT-010 — hero sub-copy now names the WhatsApp ordering path + "heirloom you'll keep forever." MKT-011 — `generateMetadata` falls back OG image to `ogImage || images[0].url` (absolute-ized) and description to the trimmed product description. **Verified** in browser: review JSON-LD renders (avg/count/review bodies), trust bullets + OG fallback on the product page, and focus-first-invalid + `role=alert` fire on both forms. **Still open in Phase 1**: page-BODY WhatsApp-number sweep (home hero/bands + about/faq/process/contact/portfolio/search/workshops still use the SITE default number, not Site Settings).
- **Page-body WhatsApp-number sweep (audit Phase 1, ENG-001 close-out)**: every page-BODY WhatsApp CTA now resolves the studio number (Site Settings) instead of the SITE constant, closing the Phase 1b gap. Server pages (home + `about`/`faq`/`process`/`contact`/`portfolio[slug]`/`search`/`workshops`/`whatsapp-order`) read `getSiteSettings()` (cache()'d — no extra query) and pass `whatsappNumber` into `buildWaLink(msg, number)`; `about`/`process` became async ISR (`revalidate=300`) to keep the number fresh without going fully dynamic. Client CTAs read the number from a new `WaNumberProvider` (mounted once in the layout, fed `settings.whatsappNumber`) via `useWaNumber()` — `contact-form`, `quick-view`, `home-hero`(prop). Both order server actions (`submitProductOrder`/`submitCustomOrder`) build their `whatsappUrl` with the settings number too. The numberless `wa.me/?text=` **share composer** in `share-buttons` is intentionally left alone (it's a share-to-friend intent, not a studio contact). **Verified** with a distinct DB number (918888…): all 9 pages + the quick-view dialog + a full E2E product order all emit the settings number, zero SITE-default leaks.
- **SEO batch (audit Phase 2a, SEO-001…008)**: SEO-001 — `/search` now sends `robots: { index:false, follow:true }` + self-canonical and is dropped from `sitemap.ts` (no more thin `?q=` index bloat). SEO-002 — `/shop/[category]` `generateMetadata` adds `alternates.canonical: /shop/${slug}` so filter/sort permutations consolidate. SEO-003 — category page emits `BreadcrumbList` + `CollectionPage`(`ItemList` of the SSR product URLs) JSON-LD. SEO-004 — product `Product` JSON-LD always carries `brand`, omits `image` when there's nothing renderable (no empty array), and enquire-only pieces carry **no fabricated offer** (priced pieces keep `AggregateOffer`). SEO-006 — root `layout.tsx` metadata gains `openGraph`(type/siteName/locale/url) + `twitter:{card:summary_large_image}`, inherited site-wide. SEO-007 — Organization/LocalBusiness schema gains a `ContactPoint`(customer service, tel/email, en+hi), `areaServed:"IN"`, and `priceRange` — no fabricated city/hours/geo (not verifiable). SEO-008 — sitemap filters categories to those with ≥1 published non-DEMO product and stamps `lastModified`/`changeFrequency` on static routes. **Verified** in browser: /search noindex + absent from sitemap (only the non-empty category listed, static routes carry lastmod); category canonical + breadcrumb/collection schema; product brand + gated image + offer-omission for enquire-only; root OG/Twitter tags; business ContactPoint/priceRange/areaServed. **Phase 2 remaining**: 2b perf (image srcset PERF-001, scope GSAP/Lenis/preloader/motion out of root layout PERF-002/004/007/008/009, drop client zod PERF-003, defer Behold + remove dead three.js PERF-005/006), 2c a11y (mobile-menu focus trap A11Y-003, skip link A11Y-005, required-field text A11Y-007, contrast A11Y-008/009, GSAP-hides-LCP UIUX-003, IG feed states UIUX-005, Quick View touch UIUX-006).
- **Motion & bundle perf (audit Phase 2b, PERF-002/004/006/007/009)**: PERF-006 — deleted the dead `components/three/` (hero-canvas + resin-scene, unused since the WebGL hero was removed) and dropped `@react-three/drei|fiber|postprocessing` + `@types/three`. **`three` stays** — it's a runtime dep of `@google/model-viewer` (the product 3D gallery viewer), so removing it broke the build until re-added. PERF-002/004/007 — `SmoothScrollProvider` (GSAP+Lenis) and `MarketingScripts` moved OUT of the root `app/layout.tsx` into the `(public)` layout, so `/studio` admin + non-public routes no longer load GSAP/Lenis/pixel (Vercel Analytics + Speed Insights stay site-wide). PERF-007 — the `Preloader` no longer locks scroll or blocks interaction (`pointer-events-none` + the scroll-lock effect removed) and its hold shortened 1400→600ms (hard-stop 2000→1200ms) per owner's "shorten, no scroll-lock" call. PERF-009 — Lenis is now **desktop-only**: it bails on `(pointer: coarse)` touch/mobile (already bailed under reduced-motion), so phones use native scroll (owner's "desktop only" call). **Verified** in browser: scroll never locked (`html` overflow visible, page scrolls during preloader), preloader overlay is `pointer-events:none`, `html.lenis` present on desktop home but **absent on /studio**, studio login renders fine, build clean. **Deferred to Phase 2b-2 (payload)**: PERF-001 image srcset, PERF-003 client-zod slimming, PERF-005 defer Behold script; PERF-008 (FloatingWhatsApp motion→CSS) is negligible while `motion` stays in the public bundle via Preloader/CursorGlow/ScrollReveal.
- **Payload perf (audit Phase 2b-2, PERF-003/005)**: PERF-005 — the Behold Instagram widget script is no longer injected eagerly on mount; `instagram-feed.tsx` now waits for an `IntersectionObserver` (rootMargin 300px) to bring the footer-adjacent feed near the viewport before loading the third-party module (falls back to a rAF-deferred load where IO is unavailable). PERF-003 — the public contact form dropped `zod`/`zodResolver` for hand-rolled react-hook-form `validate` rules (same messages, same trim semantics, same `aria-describedby`/`role=alert`/focus-first-error from Phase 1d). contact-form was the **only public** client zod importer (studio forms still use zod → admin bundle only), so this removes zod from the public client bundle. **Verified** in browser: Behold script absent at page top, injected + `<behold-widget>` renders after scrolling near the feed; contact validation fires identical errors (empty/invalid phone/invalid email/short message) with focus + aria intact, valid input clears all field errors. **Phase 2 remaining**: 2b-3 PERF-001 (image srcset — own PR, needs Vercel/Cloudinary preview to verify resizing); 2c a11y (A11Y-003/005/007/008/009, UIUX-003/005/006).
- **Responsive images (audit Phase 2b-3, PERF-001)**: the large hero/homepage-feature/about-story/category-hero Cloudinary images shipped a single full-res width (`unoptimized`, no srcset). New `lib/cloudinary.ts` (`cloudinaryLoader` injects `f_auto,q_${q},w_${width},c_limit` into the `/upload/` transform; `isCloudinary()` returns boolean — NOT a type guard, else its false branch narrows non-Cloudinary strings to `never`) + a **client** `components/media/responsive-image.tsx` (`ResponsiveImage`): Cloudinary src → the width-injecting loader (real srcset resized at Cloudinary's CDN, no Next optimizer round-trip — best for the LCP hero); `/uploads` → `unoptimized`; other hosts → Next's optimizer. Client-only because a `loader` function can't cross the RSC boundary (server `<Image loader=…>` throws "Functions cannot be passed to Client Components"). Applied at home-hero, homepage-feature, about-story, and both category heroes (home rail + `/shop/[category]`); removed the now-unused `servesDirectly` call sites. **Verified**: hero emits an 8-width srcset (`w_640…w_3840`), feature/about/category URLs carry `f_auto,q_auto,w_<n>,c_limit`. DB product/blog/portfolio card images already ship srcset via Next's optimizer (allowlisted `res.cloudinary.com`), so they were left as-is; a full `ResponsiveImage` unification is a possible follow-up. **Phase 2 remaining**: 2c a11y (A11Y-003/005/007/008/009, UIUX-003/005/006). **RETIRED in Phase 7 (b1cf3e6):** `ResponsiveImage`/`cloudinaryLoader` were removed with the v7 layer, and the orphaned `lib/cloudinary.ts` was deleted in the 2026-08-21 re-audit (bffb0e2) — images now go through `next/image` and the standard optimizer paths.
- **A11y quick wins (audit Phase 2c-a, A11Y-005/007/008/009)**: A11Y-005 — a `sr-only focus:not-sr-only` **skip-to-content** link is the first focusable element in BOTH the `(public)` layout (`#main-content`) and the studio dashboard layout (`#studio-content`), with matching `id`s on `<main>`. A11Y-007 — order-panel required fields (name, phone, custom SELECT + SWATCH/SIZE) now carry a `sr-only "(required)"` in the label; name/phone inputs get `required`+`aria-required`, SELECT triggers get `aria-required` (dropped from `role="group"` chip divs — invalid ARIA there, the label text covers them). A11Y-008 — search input placeholder raised `text-porcelain/40 → /60` (~4.5:1). A11Y-009 — `--header-ink-soft` raised `0.72 → 0.85` (both light + dark header themes) so 13px announcement/nav soft ink clears AA over hero backdrops. **Verified**: Tab focuses the skip link (visible on focus) → Enter jumps to `#main-content`; `<main id>` present in both layouts; order name/phone report `aria-required="true"` + "(required)"; placeholder is `/60`; build/lint clean. **Phase 2c-b (behavioral, remaining)**: A11Y-003 mobile-menu focus trap, UIUX-003 GSAP-hides-hero-LCP, UIUX-005/A11Y-006 IG-feed states+fallback, UIUX-006 Quick-View touch discoverability.
- **A11y behavioral (audit Phase 2c-b, A11Y-003/006 · UIUX-003/005/006 — completes Phase 2)**: A11Y-003 — the hand-rolled mobile-menu dialog (`dynamic-header.tsx`) now traps Tab/Shift+Tab inside `#mobile-menu` (wraps first↔last, pulls stray focus back) and marks the sibling `<header>` `inert` while open, so keyboard/AT users can't reach the obscured page. UIUX-003 — `KineticHeading` capped its `document.fonts.ready` wait with `Promise.race([fonts.ready, 400ms])` so a slow webfont can't leave the hero h1 (LCP) `autoAlpha:0` indefinitely. UIUX-005/A11Y-006 — `instagram-feed.tsx` now reserves `min-h-[220px]` (no CLS), shows an `animate-pulse` skeleton while loading, and after ~3.5s of the `<behold-widget>` failing to upgrade (MutationObserver watches for hydration) shows a labeled static fallback ("Our latest work lives on Instagram" + a "View us on Instagram" button when `settings.socials.instagram` is set); region has `role="region" aria-label`. UIUX-006 — the product-card Quick-View slot is `pointer-events-none` while `opacity-0` (no phantom corner-tap intercept on hover devices) and `[@media(hover:none)]` shows+enables it so it's discoverable/tappable on touch. **Verified** (Playwright): Tab stays inside the open menu + header inert; hero h1 opacity=1 by 550ms; IG region shows skeleton then the aria-labeled fallback when the script is blocked (sandbox); quick-view slot is opacity1/pointer-events auto under `hover:none`. lucide dropped brand icons → inline Instagram SVG (matches footer). **Phase 2 COMPLETE** (2a SEO + 2b/2b-2/2b-3 perf + 2c-a/2c-b a11y). Next: **Phase 3** — design-system consolidation (gold-ink token, `headerTheme` revival, tokenize hex, product-form split, ESLint cleanup).
- **Design tokens (audit Phase 3a, DS-001/004/007)**: DS-001/004 — new **theme-aware `--gold-ink`** token: `#8a6d1a` under `:root` (studio/light, where it clears AA) and `#e3c56a` under `.dark` (a lighter gold that clears AA on the public #101319 canvas); exposed as `--color-gold-ink` in `@theme inline`. Replaced all 5 hardcoded `text-[#8a6d1a]` (badge gold variant, product draft banner, product-form, two scraper dialogs) with `text-gold-ink`. DS-007 — new **`--whatsapp: #25d366`** token (`--color-whatsapp`) per the owner's "green live-chat accent" decision: the floating button's `hover:bg-[#25D366]` → `hover:bg-whatsapp`, and the footer "Chat on WhatsApp" icon → `text-whatsapp`. **Verified**: the "Made in India" gold badge computes `rgb(227,197,106)` (light gold, AA) on the dark home page instead of the old muddy `#8a6d1a`; `--gold-ink`/`--whatsapp` resolve on `.dark`; footer chat icon + badge carry the token classes. **Phase 3 remaining**: the DS-002/003 all-dark codemod (rename Section tones truthfully + delete dead `headerTheme`/dark-ink header machinery — owner chose all-dark), DS-006 off-white/void single-sourcing; **3b** eyebrow/StudioTableHead/order-panel-hex (DS-005/008/009); **3c** ESLint/untrack-prisma/product-form-split + UIUX-004/009.
- **Component tokenization (audit Phase 3b, DS-005/009)**: DS-005 — the 5 inline `.eyebrow` clones (`text-[13px] lowercase tracking-[0.18em] text-azure`, which dropped the utility's `font-weight:550`) replaced with the `eyebrow` utility in `error.tsx`, `not-found.tsx`, and `page.tsx`×3 (home-hero's `0.14em`/porcelain stat list is a different style, left alone). DS-009 — new `lib/brand-colors.ts` (`BRAND` = TS mirror of the globals.css palette hex, for the layers CSS vars can't reach) now backs order-panel's `SWATCH_COLORS` brand entries (blue/navy/azure/ocean/gold + the sapphire fallback) so brand blue/navy/gold have one source; the ~45 genuine product-color swatches stay literal data. **Verified**: build/tsc/eslint clean; `BRAND` values mirror the tokens exactly (visual no-op, token discipline only). **Phase 3 remaining**: DS-002/003 all-dark codemod (tone rename + dead `headerTheme` removal — owner chose all-dark; big codemod, wants CI/preview), DS-006 (off-white/void single-source via `brand-colors.ts`), DS-008 (StudioTableHead across ~15 tables), **3c** ENG-004 ESLint / ENG-005 untrack generated prisma / ENG-006 product-form split + UIUX-004/009.
- **Dead headerTheme prop removed (audit Phase 3c-1, DS-002 API part)**: `Section` declared `headerTheme: "dark"|"light"` as REQUIRED then discarded it (`void headerTheme`) — a misleading no-op that every one of 71 call sites across 23 files had to pass. Removed the prop from `SectionProps` + the component + all 71 call sites (perl codemod, prettier-tidied). The public site is all-dark, so `Section` still hardcodes `data-header-theme="dark"` and `DynamicHeader` computes light ink exactly as before — **zero visual change** (verified: header `data-ink="light"`, sections still emit `data-header-theme="dark"`, no stray `headertheme` DOM attrs / React unknown-prop warnings; build prerenders all 48 pages). **Phase 3 remaining**: DS-003 tone rename (porcelain→base, ice→panel — Section-only, liquid-glass uses other tone values) + delete the now-fully-dead `INK_VARS.dark`/`SURFACE.dark`/observer theme-swap machinery in `dynamic-header.tsx`; DS-006 off-white/void single-source; DS-008 StudioTableHead; 3c (ENG-004/005/006, UIUX-004/009).
- **Tone tokens renamed (audit Phase 3c-2, DS-003)**: `Section` tone tokens now describe the surface they render instead of lying — `porcelain`→`base` (rich-black canvas, `bg-background`) and `ice`→`panel` (lifted card, `bg-card`); `midnight`/`deep-ocean`/`void`/`transparent` were already truthful and unchanged. Codemod across 18 files (28 `base` + 12 `panel`); TONE_CLASSES keys + default `tone="base"` updated; `SectionTone` type follows the keys so tsc verified every call site was renamed. `porcelain`/`ice` were Section-ONLY (liquid-glass's `tone` is `"light"|"dark"`), so the rename is a pure alias change — **visual no-op** (CSS output `bg-background`/`bg-card` identical). Stale `porcelain band`/`ice band` code comments left as-is (cosmetic). **Phase 3 remaining**: 3c-3 delete the now fully-dead `INK_VARS.dark`/`SURFACE.dark`/IntersectionObserver ink-swap in `dynamic-header.tsx` (hardcode light ink); DS-006 off-white/void single-source; DS-008 StudioTableHead; 3c-4 (ENG-004/005/006, UIUX-004/009).
- **Dead header ink-machinery removed (audit Phase 3c-3, DS-002 finish)**: with every Section now emitting `data-header-theme="dark"` (all-dark), `themeToInk` always returned "light", so `dynamic-header.tsx`'s entire section-aware ink system was dead. Removed the `Ink` type, `themeToInk`, `INK_VARS.dark`/`SURFACE.dark` branches, the `ink` useState + all hysteresis machinery (commitInk/queueInkCandidate/inkRef/pendingInkRef/rAF/retry/MIN_COMMIT_GAP_MS), and the whole IntersectionObserver + MutationObserver + resize ink-swap effect (−196/+25 lines). Header now hardcodes light ink (`HEADER_INK`) over a single midnight-blue glass surface (`HEADER_SURFACE`) that still fades in on scroll; `OVERLAY_INK`(mobile menu)=`HEADER_INK`; dropped the now-unused `useCallback` import. Section still emits the (now-inert) `data-header-theme="dark"` — harmless, left for a later tidy. **Verified** (Playwright): header `data-ink="light"` / `--header-ink #f8f9fa` at top with glass opacity 0; scrolled 2000px → still light + `data-scrolled` + glass opacity 1; mobile menu opens with light overlay ink. build/tsc/eslint clean. **Phase 3 remaining**: 3c-4 DS-006 off-white/void single-source + DS-008 StudioTableHead; 3c-5 ENG-004/005/006 + UIUX-004/009.
- **Off-white/void single-sourced (audit Phase 3c-4, DS-006)**: three drifted color literals were scattered across the inline/Satori/error layers CSS vars can't reach — the warm off-white `#F7F7F4`/`rgba(247,247,244,α)` (vs the porcelain token `#f8f9fa`), the deep blue-black `#05080F` OG/error canvas (vs neutral `--void #0a0a0a`), plus raw `#0A1A2F`/`#0F52BA`/`#D4AF37` restatements of existing `BRAND` hexes. `lib/brand-colors.ts` grew a named **`voidBlue: "#05080f"`** (documented as intentionally bluer than `void`) and a **`porcelainAlpha(α)`** helper (derives `rgba()` from `BRAND.porcelain` so soft-white never drifts again). Swapped literals → `BRAND.*`/`porcelainAlpha()` in all 3 OG images (`opengraph-image` ×default/product/blog), `global-error.tsx`, public `error.tsx`/`not-found.tsx` ghost strokes, and `manifest.ts` (theme/background). Separately, `dynamic-header.tsx` **dropped its inline `HEADER_INK`/`OVERLAY_INK` override entirely** — the header + mobile overlay both live inside the layout's `.dark` wrapper, so ink now inherits straight from the `.dark` token layer (`--header-ink` porcelain, `--header-ink-soft` porcelain @0.85); this also completes A11Y-009 (the old inline value forced the softer 0.75). **Verified** (Playwright): header `--header-ink #f8f9fa` / `--header-ink-soft #f8f9fad9` (=0.85) at top with glass opacity 0, `data-scrolled` + glass opacity 1 scrolled, mobile overlay inherits the same tokens, 404 stroke = `rgba(248,249,250,0.25)`; default OG route renders a 200 image/png; build/tsc/eslint clean. **Phase 3 remaining**: DS-008 StudioTableHead (18 studio table heads); 3c-5 ENG-004/005/006 + UIUX-004/009.
- **StudioTableHead primitive (audit Phase 3c-4b, DS-008)**: the identical `<tr className="border-b border-foreground/8 text-left text-[11px] uppercase tracking-wider text-muted-foreground">` header row (with the arbitrary `text-[11px]`) was copy-pasted onto 18 studio list-table heads across 17 files. New `components/studio/studio-table-head.tsx` — `<StudioTableHead>` renders that `<tr>` once (via `cn(shared, className)`), wrapping **only** the row so each table keeps its own `<thead>` (one, `all-websites.tsx`, is `sticky top-0` — would have broken a `<thead>`-wrapping primitive). Codemod swapped all 18 rows + added the import (+53/−36, ~+3/−2 per file); ran the perl swap only (no blanket `prettier --write`, which had reformatted pre-existing unrelated indentation — reverted that noise). **Verified**: tsc/eslint/`next build` (48 pages) clean; logged into the studio (Playwright) and both `/studio/categories` (6 `<th>`) and `/studio/faqs` (5 `<th>`) render `<thead><tr>` with the exact shared class string — byte-identical output. **Phase 3 remaining**: 3c-5 ENG-004 (ESLint warnings) / ENG-005 (untrack generated prisma) / ENG-006 (split product-form.tsx) + UIUX-004/009.
- **ESLint warnings cleared (audit Phase 3c-5a, ENG-004 · ENG-005 verified done)**: `eslint src` reported 6 warnings — one dead `useEffect` import in `confirm-delete-dialog.tsx` and five `react-hooks/incompatible-library` React-Compiler bailouts where destructured `watch()` from react-hook-form opts the whole form component out of auto-memoization. Removed the dead import and converted every render-feeding `watch("x")` to the compiler-friendly `useWatch({ control, name: "x" })` across `settings-form` (1), `blog-post-form` (1), `page-form` (2), `portfolio-form` (3) and `product-form` (6); the one `watch("categoryId")` read inside a product-form onClick became `getValues("categoryId")` (a one-shot read, no subscription). `eslint src` now exits clean with **zero warnings**, so real warnings surface. ENG-005 (untrack generated Prisma client) was already resolved — `git ls-files src/generated` returns 0 files and `.gitignore` lists `/src/generated`, so it's a verified no-op. **Verified** (Playwright, authenticated studio): page-form slug preview live-updates to `/hello-world-test` on title input (useWatch fires); product-form `/new` renders all 6 conversions + the getValues handler with its price band reflecting typed priceMin/priceMax; settings-form renders — no React/runtime errors (only sandbox `ERR_TUNNEL` for blocked outbound assets). tsc clean. **Phase 3 remaining**: ENG-006 (split 954-line product-form.tsx), UIUX-004 (FloatingWhatsApp vs sticky mobile CTA), UIUX-009 (character counters).
- **Floating WhatsApp vs sticky CTA (audit Phase 3c-5b, UIUX-004)**: on mobile product pages the global `FloatingWhatsApp` FAB (`fixed bottom-5 right-5 z-40`) sat directly over the `StickyMobileCta` bar's "Customize" button (`fixed bottom-0 z-30 lg:hidden`) — two primary CTAs colliding on the highest-conversion template. The FAB now reads `usePathname()`: on `/product/*` routes it lifts to `bottom-24 lg:bottom-5` (clears the ~60px bar on mobile; desktop hides the bar so it drops back to the corner); everywhere else it stays `bottom-5`. The owner values the WhatsApp path ("green live-chat accent"), so the FAB is raised rather than hidden. **Verified** (Playwright): mobile product page → FAB bottom = 96px, sticky bar visible, **no overlap** with Customize; mobile home → FAB 20px, no bar; desktop product → `lg:bottom-5` wins (20px), bar hidden. tsc/eslint clean. **Phase 3 remaining**: ENG-006 (split 954-line product-form.tsx), UIUX-009 (character counters).
- **product-form.tsx split (audit Phase 3c-5c, ENG-006)**: the 954-line client monolith is now a **141-line orchestrator** + a `products/product-form/` folder of focused pieces: `schema.ts` (types, zod `formSchema`, `FIELD_TYPES`, and the two mappers `buildDefaultValues`/`buildUpsertPayload`), `form-section.tsx` (shared `FormSection`/`FieldError`), and one component per band — `RewriteWarning`, `EssentialsSection`, `PricingSpecsSection`, `OccasionsSection`, `CareNotesSection`, `MediaSection`, `CustomizationFieldsSection`, `SeoSection`. The orchestrator holds `useForm` + `FormProvider` and the save/delete state; every section pulls `register`/`control`/`errors` from `useFormContext<FormValues>()` (no prop-drilling), except `MediaSection` which takes `uploading`/`setUploading` so the sticky save bar can disable during uploads, and the two sections needing `categories` take it as a prop. Public API unchanged — `ProductForm` + `ProductFormInitial` still export from `product-form.tsx`, so `products/new` and `products/[id]` are untouched. Pure structural refactor, behaviour identical. **Verified** (Playwright, authenticated studio): CREATE renders all 7 sections, live price preview (`₹5,000 – ₹9,000`), category Select + occasion toggle + custom-field-array add all work, submit persists a row and redirects to the new product's edit page; EDIT loads populated defaults + the product-only Delete/preview UI and Save updates the same row ("Product saved"). tsc/eslint clean, no page errors. **Phase 3 remaining**: UIUX-009 (character counters on maxLength fields).
- **Character counters (audit Phase 3c-5d, UIUX-009 — completes Phase 3)**: the public-funnel long-text fields capped silently (`maxLength` with no feedback), and the contact message had a zod `.max(2000)` but no client `maxLength` — so users only learned the limit via a post-submit error. New `components/ui/char-counter.tsx` (`CharCounter` — a right-aligned `n/max` line that flips `text-muted-foreground → text-destructive` at ≥90% of the cap) now sits under each capped field: custom-order design-idea (2000) + notes (1500), order-panel notes (1500), and the contact message — which also gained `maxLength={2000}` so the client cap matches the server. Contact reads its live length via `useWatch({ control, name: "message" })` (compiler-friendly, no `watch()` bailout); the controlled forms read `value.length` directly. **Verified** (Playwright): every counter live-updates while typing, the contact counter turns destructive at 1950/2000, and `maxLength` caps a 2100-char paste at 2000. tsc/eslint clean. **Phase 3 (design-system consolidation) COMPLETE** — DS-001…009, ENG-004/005/006, UIUX-004/009 all shipped.
- **Tier-1 owner catalog import + image-src hardening**: the owner's `resinriva2.0` Google Sheet (Sheet1/gid=0 = the scraper's Tier-1 "Owner's list" staging tab, 12 products in the Bulk Import `products` schema) is now snapshotted at `docs/import/tier1-owner-products.csv` with a runbook (`docs/import/README.md`) for running it through Studio → Import on the live site (sheet is private → 403 for the sheet-link route unless link-sharing is enabled; the CSV upload path always works). One data fix vs the sheet: the Christmas Bells row's junk auto-slug `untitled-sep30_22-17` (rejected by the importer's URL-safety rule) → `personalized-lithophane-christmas-bells-ornaments`. **Hardening (new `lib/image-src.ts`)**: `mirrorProductImage` deliberately keeps the source-store URL when a mirror fails, but every renderer passed such URLs through Next's optimizer, which **throws** for hosts outside `images.remotePatterns` — `isOptimizableImageSrc(src)` (mirrors the remotePatterns allowlist: Cloudinary + Vercel Blob optimizable; `/uploads` + any other remote host/scheme → `unoptimized`) now backs all 20 former `startsWith("/uploads")` sites + `ResponsiveImage`, and adds protection to 7 previously-unguarded `next/image` sites (studio product/portfolio lists + form previews, inquiry reference images). **Verified E2E** (Playwright, real wizard): CSV → Preview (11 create + 1 slug error → fixed row re-imported: 1 create) → Run: **12/12 created** with 48 gallery-image rows; studio list renders all 12 external-host thumbnails as raw unoptimized `<img>` (0 optimizer URLs, no crash — pre-fix this crashed) and `?preview=1` product page renders gallery + ₹ band. All rows are **DRAFT** with source-store copy + $-looking prices — owner must rewrite/re-price before publishing (see runbook). tsc/eslint/`next build` clean.
- **Audit loose ends (ENG-007 finish · UIUX-010 verified done) + ui-ux-pro-max review**: ENG-007's remaining gap — the studio Settings `address` field was collected but never reached the structured data — is closed: `getSiteSettings()` now exposes `address`, and the public layout fills the LocalBusiness `PostalAddress.streetAddress` from it when set (Organization untouched; empty address keeps the old country-only shape). UIUX-010 needed no code: the footer Instagram icon already renders only with a real profile URL and `sameAs` already flows from settings (verified in-browser: with socials+address seeded, Organization/LocalBusiness both carry `sameAs` and only LocalBusiness the street address). Separately, a full **ui-ux-pro-max review** of the public site (5 pages × 2 viewports, Playwright-measured, every finding adversarially re-verified: 45 raw → 37 CONFIRMED — 6 Critical / 19 Major / 9 Minor / 3 Polish) is committed at `docs/audit-uiux.md`; headline items: brand-sapphire text fails AA site-wide (2.6–2.8:1), quick-view buttons collapse to 20px on mobile, 16×16 dialog close target, product-page 170px horizontal overflow from the 6-thumb rail, order-form focus/announcement gaps. Fix waves not yet started — [UIUX-P01…P06] Critical set is the natural next PR.
- **UI/UX fix program — all 37 `docs/audit-uiux.md` findings implemented ([UIUX-P01…P37])**: executed as 6 file-disjoint fix waves (shared primitives / shop / product / custom-order / contact / home+footer) over a token foundation: new theme-aware **`--sapphire-ink`** text token (light `#0f52ba` 7.2:1, `.dark` azure `#3b82f6` 4.75:1+) swapped into every failing text-sapphire site incl. the Button outline/link variants (P01), and a global `input/select/textarea/[data-slot=select-trigger] { scroll-margin-top: 8.5rem }` so focused/errored fields clear the fixed header (P05/P22). Highlights: 44px touch targets across header/footer/breadcrumbs/dialog-close/selects/share/sticky-CTA/uploader/quick-view (P03/07/08/09/10/11/12/13/25 — hit-area extensions via min-h-11, `after:-inset` pseudo hits, py+negative-margin, `relative z-10`); product page `min-w-0` kills the 170px mobile overflow (P06); quick-view buttons `w-full sm:flex-1` (P02); gallery focus ring as an `::after` overlay that paints above the fill image (P16) + `onCloseAutoFocus` returns lightbox focus to the stage (P17); sticky bar hides (translate+inert) while `#order-panel` OR the footer is on screen + `pb-20` reserve (P21); custom-order: submit focuses+scrolls first invalid field (P05), error ids + `aria-describedby` + `role=alert` (P19), `aria-required` (P20), validate-on-change-after-error (P28), mobile 3-step explainer (P23); FAB lifted on /custom-order too (P14); shop live region + sr-only h2 + `type=search` (P18/26/31); header surface goes ~opaque when scrolled (P22); contact `onTouched` validation, no mid-token email split, distinct hero-pill copy (P29/33/37); footer/newsletter label + refocus (P30), IG fallback always actionable via WhatsApp CTA (P24), collections rail prev/next on desktop via new client `collections-rail.tsx` (P34); counters announce near-limit with text (P35); numerals aria-hidden at /35 (P36); sr-only "(opens in new tab)" on every `target=_blank` (P27). **Verified**: 44-check re-measurement workflow — 37 PASS on first pass, 7 stragglers root-caused and fixed (2 page-local breadcrumbs the shared fix missed; P16 outline under the fill image → overlay ring; P21 footer occlusion → footer IO; P09 caption overlap → `relative`; P05/P22 were a stale `.next` dev-cache artifact — rule serves at 136px after clean restart), then re-verified PASS. P25 note: 44px boxes; where the trail wraps, adjacent crumbs share bleed rows (each keeps ≥21px exclusive — passes 2.5.8 the same way the report scored it). tsc/eslint/`next build` clean.
- **Security baseline (audit Phase 0)**: `next.config.ts` `headers()` — enforced HSTS/nosniff/Referrer-Policy/Permissions-Policy/X-Frame-Options (SAMEORIGIN globally, **DENY on `/studio`**) + a **report-only CSP** (self + Cloudinary/Blob/Instagram/Behold/Vercel-analytics; rename to `Content-Security-Policy` to enforce once the report stream is clean). `src/lib/env.ts` (Zod env validation, required DATABASE_URL/AUTH_SECRET, imported by `db.ts` → fails fast at boot). `rateLimit` now has a `record:false` peek so the login (`auth.ts` `authorize`) throttles per-email(5)/per-IP(15) per 15min on **failures only**; password-reset (`auth-public.ts`) is IP-rate-limited + token cooldown; the public `/api/upload` endpoint is IP-rate-limited across BOTH branches. SVG dropped from `ACCEPTED_UPLOAD_TYPES` (+ nosniff/attachment/`default-src 'none'` on the `/uploads` route). `tiptap-render.ts` sanitizes link hrefs (scheme allowlist) before `dangerouslySetInnerHTML`. Scraper CSV export uses `requireStaff`. Order `referenceUrlSchema` restricts to the Blob host / `/uploads/`. Contact email uses `studioFrom()`. In-memory limiter is per-instance (SEC-005 durable-store follow-up); **key rotation SEC-009 is owner-side**.
- **Verified in browser (6/6)**: anon redirect w/ callbackUrl · wrong-password error · login → dashboard · role shown · login bounce when signed in · sign-out re-locks. Build/lint/typecheck clean.

### ✅ Phase 4 — Admin Core (complete)
- **Shell**: `studio/(dashboard)/layout.tsx` — midnight sidebar (`components/studio/sidebar.tsx`, phase-gated nav items disabled with phase chips), mobile Sheet nav, sonner `<Toaster/>`, user chip + sign-out. Login stays bare outside the group.
- **Shared studio kit**: `storage.ts` (putFile/deleteFile — Vercel Blob driver when BLOB_READ_WRITE_TOKEN, else local `public/uploads` + **dev fallback route `app/uploads/[...path]/route.ts`** since `next start` won't serve post-build public files), `slug.ts`, `activity.ts` (logActivity), `actions/helpers.ts` (requireStaff, runAction→ActionResult), `hooks/use-selection.ts`, `components/studio/`: BulkBar, ConfirmDeleteDialog (exact spec copy, type-DELETE >10, y→ies pluralization), PageHeader/EmptyState. next.config: serverActions bodySizeLimit 12mb + Blob image remotePatterns.
- **Dashboard**: stat cards ($transaction counts), recent 6 inquiries w/ status badges, quick actions, owner-fed-catalog banner.
- **Categories**: actions (upsert w/ immutable slug, delete blocked while products exist w/ named error, index-space reorder that self-normalizes duplicate orders), list w/ counts + arrows + dialog form (remount-keyed body — NO sync-state effects, lint-enforced), NewCategoryButton export.
- **Products**: `actions/products.ts` — upsertProduct (zod, replace-all images/customFields in one $transaction, **publish guard: needsRewrite && !confirmRewrite → exact spec error; confirmRewrite clears flag**, uniqueSlug create-only), setProductsStatus (publish skips needsRewrite rows → {updated, skippedRewrite}), deleteProducts (cascades + best-effort Media/file cleanup by url→pathname), toggleFeatured. List: q/status/category URL filters, thumbnails, DEMO + needs-rewrite badges, featured star, BulkBar publish/draft/delete. Form (`product-form.tsx`, RHF+zod): essentials/pricing/occasions chips (OCCASIONS const in `components/studio/products/occasions.ts`: Wedding, Anniversary, Diwali, Birthday, Corporate, Housewarming, Baby)/care notes/gallery manager (upload via uploadMediaFiles, alt+order+remove)/videoUrl+model3dUrl/**Custom Form Builder** (label/type/options/required/help/order)/SEO/rewrite banner+checkbox; sticky save bar; create→redirect to edit; draft preview link `/product/<slug>?preview=1` (page arrives Phase 7).
- **Media Library**: `actions/media.ts` — uploadMediaFiles(FormData{files,folder}) validating ACCEPTED_UPLOAD_TYPES + size caps, deleteMediaItems (file+row); grid with folder chips (products/blog/portfolio/site/refs/other), copy-URL, selection + bulk delete.
- **Verified in browser (11/11)**: login → dashboard → 16 seeded categories → create category → product form (category select, price band, occasion chip, custom field, PNG gallery upload w/ preview) → create redirect → search filter → bulk publish → media listing → spec delete dialog → delete (incl. storage cleanup, proven by Media rows vanishing). Build/lint/typecheck clean.

### ✅ Post-build audit & feature program (2026-07 → 2026-08, summarized — full detail lives in audit.md + audit-implementation-plan.md + git log)
- **Master audit remediation Phases 0–4** (branch `claude/resinriva-master-audit-bm5a0f`, PRs #45–#49): security baseline (durable login rate-limit store `RateLimitHit`, SSRF guards, authz least-privilege, CSP report-only + HSTS/nosniff headers), funnel measurement (UTM attribution, pixel events, notifications), studio workflows, performance & DS remediation. Status register: audit.md Appendix C.
- **T1 dual theme (public)**: light/dark toggle, `rr-theme` localStorage + pre-paint script on `#rr-site-root` (SSR dark default, no FOUC), `.dark`/`:root` token pairs. Studio stays light-only by owner decision.
- **I-series i18n (9 locales: en default + hi gu ar es de fr zh ja)**: next-intl v4, `/[locale]` routing (`as-needed` prefix), full chrome+page+catalog translations (371-leaf parity across messages/*.json), studio per-language editing UI, RTL for ar via `dir` on the site root.
- **2026-08-09 session** (branch `claude/resinriva-audit-tasks-tfz6cs`): I4 full per-page hreflang + localized sitemap (per-locale entries, reciprocal `alternates.languages`) + `localeDetection: true`; I5 localized WhatsApp funnel (`WhatsApp` message namespace ×9, bilingual order labels via `localizedOrderLabels`, locale field on order actions, localized greetings at every chrome CTA) + per-script fonts (`src/app/fonts.ts` — Noto Devanagari/Gujarati/Arabic/JP/SC sharing `--font-script`, applied per locale on the site root) + T2 footer ThemeToggle; S4 imagery (5 category + 55 blog covers generated via Higgsfield seedream_v5_pro, 70 credits — prompts + inventory in docs/image-inventory.md, wired through `prisma/generated-cover.ts` local-first with public-CDN fallback, mirror script `scripts/mirror-generated-images.mjs`). Step-5 re-audit: audit.md Appendix D. **Blog covers are no longer placeholders.**

## 2. PENDING FEATURES (by phase)
### ✅ Phase 5 — Admin Rest (complete; 13/13 browser checks)
- **RichTextEditor** (`components/studio/rich-text-editor.tsx`): shared Tiptap 3 editor — contract `{value: unknown; onChange:(json:object)=>void; placeholder?}`; StarterKit (link disabled there, @tiptap/extension-link openOnClick:false), Image, Placeholder; toolbar w/ useEditorState selectors; immediatelyRender:false; self-contained prose CSS (no typography plugin).
- **Blog** (`actions/blog.ts`, /studio/blog[,/new,/[id]]): posts CRUD (uniqueSlug create-only, publishedAt ISO round-trip via module-level helpers, publishing stamps missing publishedAt), categories+tags create-if-missing by slugified name (returns {id,created}), tag set/connect, q+status filters, tabs Posts|Categories|Tags, cover upload folder "blog", preview link /blog/<slug>?preview=1 (Phase 8).
- **Portfolio** (`actions/portfolio.ts`, /studio/portfolio…): CRUD w/ before/after uploads, resultsMeta cleaned to {type,material,size,timeline}, gallery replace-all, delete cleans Media/files for gallery + before/after urls, category nullable ("NONE" sentinel — Radix Select forbids empty values).
- **Inquiries** (/studio/inquiries[,/[id]]): read-only + status workflow (NEW→CONTACTED→CONFIRMED→DELIVERED), per-status stat cards, source/status/q filters, bulk status buttons + delete; detail: customer card (tel:/mailto:), selections dl, reference image links, whatsappMessage <pre> + Copy + "Open in WhatsApp" (wa.me rebuild), status Select. Labels/badge-variant maps in `components/studio/inquiries/labels.ts`.
- **Testimonials + FAQs**: classic CRUDs w/ dialog forms (remount-keyed bodies), order arrows (index-space), ratings ★, avatar upload folder "site".
- **Pages** (/studio/pages…): Tiptap-edited; slugs immutable on edit; **privacy/terms deletion refused** (load-bearing legal pages).
- **Settings** (/studio/settings): full SiteSettings form (whatsappNumber zod ^[0-9]{8,15}$, socials, defaultSeo, defaultCareNotes, logo/hero uploads). **SEO** (/studio/seo): edits defaultSeo + pointers to per-entity fields. Both via updateSiteSettings upsert id "main".
- **Users** (ADMIN-only actions): create (bcrypt 12), role change + password reset, last-admin + self-delete guards; non-admins see "Admins only" EmptyState. **Activity** (/studio/activity): paginated 50/page, entity filter, read-only.
- **Bulk Import** (/studio/import, `lib/import/{templates,parse,validate}.ts`, `actions/import.ts`): 3-step wizard (type cards + template download → Google Sheet URL (docs.google.com CSV export w/ friendly sharing error) or CSV/XLSX upload → validation preview table (create/update/error per row, dupe-slug + unknown-category + enum + url checks) → batch upsert BY SLUG (≤500 rows) → report {created,updated,skipped,errors} + ActivityLog). Markdown→Tiptap via marked+@tiptap/html generateJSON. Product rows: category resolve, images mirrored into storage (non-fatal fallback to source url), custom1..6_* → CustomizationFields. **SCRAPER-EXPORT GUARD verified in browser: files containing sourceKey/externalId/contentHash headers are rejected whole-file with the review-flow message.**
- Verified 13/13: blog Tiptap create+search, testimonial dialog, seeded FAQs, legal pages listed, settings load (917096036250), users, inquiries empty state, import preview→run→rows inserted, scraper guard, activity log (22 entries).
### ✅ Phase 5B — Product Scraper (complete; full pipeline verified end-to-end)
- **Shared lib** `src/lib/scraper/`: types.ts (RichProduct ScrapeDeck v4 schema, Adapter contract, SCRAPER_UA research bot, caps: 2 pages/invocation, 40 pages/job, 150 JSON-LD products/run, 700/900ms delays), fingerprint.ts (products.json → wc/store/v1 → JSON-LD probe; marketplace blocklist amazon/etsy/flipkart/meesho/indiamart/tradeindia), hash.ts (sha1-16 over title+prices+status+images only).
- **Adapters** `adapters/{shopify,woocommerce,jsonld,strip-html,index}.ts`: Shopify 250/page major-unit prices; Woo Store API 100/page minor-unit conversion via currency_minor_unit; JSON-LD sitemap(-index, product-sitemaps preferred) → 8 URLs/page, tolerant @graph parsing, single-product failures skipped. getAdapter(platform). Malformed products skipped; only listing-level HTTP errors throw.
- **Registry** (/studio/scraper/sources): SEED_SOURCES **115 entries** (8 OWNER incl. poonam-shah-art disabled "no scrapeable catalog", 58 RESIN_GOODS, 20 SUPPLIES supply:true, 29 PRINT3D — India + global, marketplaces excluded; the 81 Phase-12 research adds carry platform only where a Shopify/Woo signal was found, else UNKNOWN so the engine fingerprints on first run). Data lives in **`src/lib/scraper/seed-data.ts`** (db-free) so `prisma/seed-sources.ts` can import it under tsx; `seed-sources.ts` keeps `applySeedSources`. **`bootstrap.ts` runs `prisma/seed-sources.ts` AND `prisma/seed-category-images.ts` on EVERY deploy** (not gated by the empty-DB guard; the latter fills each catalog category's cover `image` from the branded Cloudinary library, non-destructively — only where `image` is still null, so it never fights owner edits) so newly-added curated sources reach an already-seeded prod DB automatically — idempotent, never downgrades verified platforms nor flips operator enable-toggles; addScrapeSource / **addScrapeSources (bulk — paste many URLs, one per line, ≤25/batch; each normalized → blocklist → live fingerprint → upsert-by-host-key; per-URL report added/updated/invalid/blocked/error)**; Add-sources dialog uses a multi-URL textarea + results view; per-row Verify; tier tabs; bulk enable/disable/delete.
- **Scraper command center** (/studio/scraper, `page.tsx` + `scraper-kpis.tsx` + `all-websites.tsx`): the landing page is now a dashboard — a 6-tile **KPI strip** (Websites/enabled, Scraped, Failed(+empty), Not run, Products staged, Pending review; health tallied server-side via `deriveHealth` over every source), the Scrape/Batch control cards, a paginated **Recent jobs** table, and an **All websites** overview showing every source in one page (client status-filter + name/URL search + sortable, scroll box with sticky header, no pagination — the deliberate exception). Rows link to the source detail page.
- **Studio-wide 50/page pagination** (`components/studio/pagination.tsx`): shared `usePagination(rows, PAGE_SIZE=50, resetKey?)` (render-time page-1 reset on filter change, derived page-clamp — no set-state-in-effect) + `<Pagination>` control (N–M of T, Prev/Next). Applied to EVERY studio list — products, WhatsApp Orders, blog posts+taxonomy, portfolio, testimonials, FAQs, pages, categories, users, media grid, scraper sources/review/jobs/per-source tables. Selection `rowIds` are derived from the visible page everywhere, so select-all/bulk act on the current page only; hand-reorder lists (FAQs/categories/testimonials) use a global index so up/down stays correct across pages.
- **Source status table** (/studio/scraper/sources): the registry is now a **sortable, paginated (50/page) status table** — each source shows a health pill (`lib/scraper/health.ts` `deriveHealth`: DISABLED / NEVER "Not run" / RUNNING / FAILED / EMPTY "No products" / OK "Scraped"), staged-product count (+ pending), platform, and last-run time. Latest job per source is joined by the **stable `sourceKey`** (via `scrapeJob.findMany({distinct:["sourceKey"], orderBy createdAt desc})`, NOT the nullable `sourceId` relation). Client-side **status filter** (incl. "Needs attention" = FAILED+EMPTY+NEVER, to surface sites that didn't scrape) + **name/URL search**; every column sorts via the shared **`useSort`/`SortHead`** primitive (`components/studio/sort-header.tsx`; nulls last, array copied, id-keyed selection survives re-sort). The main jobs table and the detail tables use the same primitive. Source name links to the detail page.
- **Source detail page** (`/studio/scraper/sources/[key]`, `source-detail.tsx`): findUnique by `key` (@unique) + generateMetadata/cache; meta card (health/platform/tier/country/notes/verifiedAt) with **Run scrape** (single-job runner polling continueScrapeJob), **Verify**, and **per-source CSV** (`/api/scraper/export?source=<key>` — new param); a sortable+searchable+review-status-filterable, paginated **products table** (all rows scraped from that source) and a sortable **run-history table**. Jobs/products both queried by `sourceKey`. The products table has **row checkboxes**, a **"Catalog category" column** (an editable per-row `<Select>` pre-filled by `lib/scraper/category-map.ts` `matchCategoryId` — deterministic keyword scoring of the source category+title → nearest catalog category, computed server-side in the page; the source site's own text stays as "Source category"), and two bulk actions: **"Add to catalog"** (opens `add-to-catalog-dialog.tsx` → `addScrapedToCatalog({items:[{id,categoryId}], mirrorImages})`, routing EACH product to its own category, with a fallback picker for anything unmapped + the copyright guard) and **"Send to Sheet1"** (→ `sendScrapedToSheet1` — links the selection into the Google Sheet's Sheet1 tab WITHOUT a DB import; env-gated, `info` toast when unconfigured). Imports become DRAFT products with `needsRewrite:true`, keyed by (importSource,importRef); the per-row import core `importOneScrapedRow` fills missing fields with **shared smart defaults** (`product-sheet.ts` `enrichScrapedFields`: care notes, SEO title/description, show-price, occasions[]) so every draft is upload-complete. On Add-to-catalog the imported products are also **auto-linked into Sheet1** (best-effort, env-gated). The **review queue reuses `approve-import-dialog.tsx`** (single category, `importApprovedScraped`). Selection is page-scoped.
- **Engine** (/studio/scraper, `actions/scraper-jobs.ts`): createScrapeJob (source or pasted URL — pasted URLs upsert into the registry), continueScrapeJob processes ≤2 pages/call (dedupe, batch-read existing by sourceKey_externalId, createMany new / transaction-update changed-hash rows / touch lastSeen unchanged), DONE on !hasMore or page cap, FAILED keeps counts, idempotent on terminal jobs; createTierJobs(tier|"ALL") in tier order; client runner drains QUEUED jobs sequentially (800ms poll, never double-enqueues), live progress strip, per-row Resume + Export CSV.
- **Scrape robustness** (why more stores now yield data): (1) **Browser UA by default** — `SCRAPER_UA` is a desktop-Chrome string (override via `SCRAPER_USER_AGENT`) because an identifying bot UA gets 403'd; robots.txt still honored. (2) **Adapter cross-fallback** — Shopify `/products.json` or Woo Store API returning 401/403/404/410 (disabled/bot-gated) or a non-JSON/non-array body falls back to `jsonldAdapter(ctx)` instead of failing the job; an empty products array is still treated as end-of-catalogue (no fallback loop); transient 429/5xx/timeouts still throw so Resume works. (3) **Robust sitemap discovery** (`lib/scraper/sitemaps.ts` `discoverProductUrls`) — tries robots.txt `Sitemap:` directives then common paths (/sitemap.xml, /sitemap_index.xml, /wp-sitemap.xml, /product-sitemap.xml, /sitemap_products_1.xml…), follows a sitemap index, never throws (missing sitemap → empty, not a failed job); used by the JSON-LD adapter and the fingerprint. (4) **HTML-signature fingerprint** — homepage Shopify (`cdn.shopify.com`, `Shopify.theme`, `shopify-section`) / Woo (`woocommerce`, `wp-content/plugins/woocommerce`) markers classify a site even when its API is bot-gated, plus a discoverable-products fallback → JSONLD; fewer UNKNOWN. (5) **robots gate** now checks the site root `/` (was the platform API path, which false-failed). **VERIFIED e2e** against a local mock store (Shopify HTML + `/products.json`→403): fingerprint→SHOPIFY, adapter 403→JSON-LD fallback→sitemap discovery→3 products staged, job DONE 3/3/0.
- **Export/Sheets**: SCRAPEDECK_COLUMNS 26 exact; /api/scraper/export?jobId|ids|reviewStatus|all=1 (auth-gated, cap 5000); sheets.ts — env-gated, accepts GOOGLE_SERVICE_ACCOUNT_JSON **or** GOOGLE_SERVICE_ACCOUNT_KEY_B64 (base64) + SCRAPE_SHEET_ID **or** SHEET_ID; RS256 JWT via node:crypto → tabs Tier1_Owner/Tier2_ResinGoods/Tier3_Supplies/Tier4_3DPrint, `syncRowsToSheet(tier, rows)` upserts by sourceKey|externalId (known keys rewritten in place, new keys appended; tab auto-created with header row). **AUTO-SYNC ON SCRAPE**: `continueScrapeJob` calls `syncSourceToSheet` when a job finishes with new/updated rows — best-effort & `isSheetSyncConfigured()`-gated (silent no-op when env unset, never fails the scrape), stamps `ScrapeJob.sheetSynced` on success. So every completed scrape pushes that source's staged products straight into its tier tab. **PRODUCT SHEET1 SYNC** (`lib/scraper/product-sheet.ts` + `sheets.ts`): a reusable `upsertRowsToTab({tab,header,rows,keyOf})` core (extracted from `syncRowsToSheet`; `columnLetter()` for the last-column, header-aware `ensureTab` that seeds a fresh Sheet1's header without clobbering data) powers `syncProductsToSheet1(rows)` — upserts into the **Sheet1** tab, merged by **slug** (column B), in the **bulk-upload "products" column format** (`PRODUCT_SHEET_COLUMNS = templateColumns(products)`, 45 cols) so linked rows re-import through the bulk uploader unchanged. `scrapedRowToProductSheetRow(row, categorySlug)` serializes a scraped row + its resolved category using the SAME `enrichScrapedFields` defaults as the DB import (no drift). Fired both automatically on Add-to-catalog and manually via the **"Send to Sheet1"** button (`sendScrapedToSheet1`, no DB import). All env-gated via `isSheetSyncConfigured()`.
- **robots.txt gate** (`src/lib/scraper/robots.ts`): per-host cached robots.txt fetch + `isPathAllowed()` (wildcards/`$`, longest-match, ties→Allow, fail-open); the engine checks it once at cursorPage 0 (SHOPIFY→/products.json, WOO→/wp-json/wc/store/v1/products, JSONLD→/sitemap.xml) and FAILs the job with a clear message if disallowed.
- **Review** (/studio/scraper/review): card grid (plain <img> referrerPolicy no-referrer) — **each card leads with its source website name (gold badge, resolved from ScrapeSource.name) + category badge**, then vertical/new-updated/status; the source filter dropdown and detail drawer ("<name> · price · last seen", "View on <name>") use the human name too; filters, detail Sheet drawer with **inline edit** (title/tagline/category/priceMin/priceMax → `updateStagedProduct`, locked once IMPORTED), bulk Approve/Reject, ApproveImportDialog (category required, mirrorImages default ON, copyright warning) → importApprovedScraped **upserts by (importSource, importRef)**: new→create DRAFT+needsRewrite:true; existing uncurated draft→update in place (replace gallery); existing curated (needsRewrite cleared)→skip untouched. Report imported/updated/skipped/errors; ScrapedProduct→IMPORTED + importedProductId.
- **VERIFIED end-to-end** (egress policy blocks the real competitor sites from THIS environment — 403 policy denial on sumaiyaresin.art etc., so verification ran against a local mock store emulating Woo + Shopify + JSON-LD; adapters/pipeline are identical, live Tier-1 runs work wherever outbound is open): registry seeds 34 · amazon.in add rejected with exact blocklist message · Woo mock scrape 7 products DONE · JSON-LD mock scrape 3 products DONE · CSV 26 columns · review grid → approve 3 → import modal (rewrite warning) → 3 DRAFT+needsRewrite products with mirrored images + IMPORTED back-refs · products list shows needs-rewrite badges · **bulk publish skipped all 3** ("Published 0 products. Skipped 3 that still need a rewrite of scraped content").
### ✅ Phase 6 — Public Core Pages (complete; 16/16 browser checks)
- **Home** (`(public)/page.tsx`, revalidate 300): 12 sections — HomeHero (`components/sections/home-hero.tsx`, isolated for the Phase 9 WebGL swap) · velocity marquee · brand story + StatCounter (`components/motion/stat-counter.tsx`, gsap count-up, reduced-motion static) · collections rail (8 DB categories, DEMO-filtered counts) · anti-grid featured pieces (12-col editorial offsets; empty → "Collection launching soon" glass card) · how-it-works 01–04 · why-ResinRiva pillars · portfolio highlights (hidden when empty) · TestimonialCarousel (embla; hidden when empty) · blog highlights (hidden when empty) · live Instagram feed (Behold widget — `components/sections/instagram-feed.tsx`, client component: injects the `w.behold.so/widget.js` module once + renders `<behold-widget feed-id>` via `createElement`) · dopamine WhatsApp CTA. ALL public product queries exclude title startsWith "DEMO".
- **About**: midnight hero → story → 4 value glass cards → void craft band → maker block ("led by Bhavya Gondaliya") → CTA. **Process**: deep-ocean hero → 6-step numbered timeline (giant sapphire/15 numerals) → materials chips → honest-timelines band → CTA.
- **Contact**: `actions/public.ts` submitContactInquiry — public, zod, honeypot (uncontrolled sr-only "website" input read via ref) + min-fill (performance.timeOrigin ref) + per-IP rateLimit 5/10min (x-forwarded-for) → Inquiry{source:CONTACT} + fire-and-forget `lib/email.ts` sendContactNotification (Resend REST, env-gated, never throws). Success swaps form for thank-you card; spam failures get a generic error. Info side: tel/wa/mailto + honest maps LINK card (no iframe — goo.gl links aren't embeddable). **FAQ**: full accordion + teaser on contact.
- **Workshops**: products of category slug "workshops" as bookable cards (wa.me book message; full form flow lands with Phase 7 product pages); elegant empty state.
- **Legal**: `lib/tiptap-render.ts` (generateHTML matching studio editor extensions) renders Page rows; **seed now writes FULL original India-appropriate Privacy (~700w) + Terms (~700w) as markdown→Tiptap and UPDATES on re-seed**; /privacy /terms with last-updated strips.
- **Error states**: root + (public) not-found (outlined 404 watermark, "waiting to be commissioned"), (public)/error.tsx, loading.tsx (pulsing wordmark), global-error.tsx (inline-styled). Root not-found re-exports the public one (unknown URLs otherwise hit Next's default).
- ISR: home/contact/faq/workshops/privacy/terms revalidate=300.
- **Header checklist re-validated on new pages** (16/16 incl. hero→light, story→dark, void→light transitions). NOTE for tests: drive scrolls with mouse.wheel (Lenis owns wheel); programmatic window.scrollTo/scrollIntoView fights Lenis's rAF loop and snaps back — this was the cause of 2 false failures.
### ✅ Phase 7 — Shop + Order Flow (complete; 17/17 E2E checks incl. full order journey)
- **Shop** (`lib/shop-filters.ts` client-safe constants + `lib/shop.ts` server lib): PRICE_BANDS (under-1k…above-50k), SORTS, buildProductWhere (PUBLISHED + NOT DEMO always; occasion via occasions {array_contains:[x]}; band overlap on priceMin/Max, null prices excluded), fetchProductsPage (id-cursor, take+1 probe, price sorts nulls-last). `actions/shop.ts` loadMoreProducts (public read, capped 12). ShopExplorer (URL-param filters via router.replace, infinite scroll sentinel + Load-more fallback, render-phase props-reset — no key needed), ProductCard (hover second-image crossfade, variant chips: SWATCH "Colours +N" / SIZE initials / SELECT "+N", quickView slot OUTSIDE the anchor), QuickView dialog (track quick_view). /shop + /shop/[category] (locked category, generateMetadata, seeded description as hero copy), revalidate 300.
- **Product page** (/product/[slug], revalidate 300): preview=1 shows drafts (banner strip); gallery client component (thumb rail, lightbox dialog, video slot, model-viewer 3D slot — dynamic import of @google/model-viewer only when model3dUrl, JSX augmented); breadcrumbs (`components/layout/breadcrumbs.tsx`); price + timeline chip; care-notes accordion (product ?? SiteSettings.defaultCareNotes); ShareButtons (wa.me share composer + copy-link, track share_product); related rail reuses ProductCard; StickyMobileCta scrolls to #order-panel; dopamine commission CTA.
- **ORDER SYSTEM**: `lib/whatsapp.ts` buildOrderMessage (EXACT spec format, pure fn shared client/server, 1500-char guard truncating ONLY notes with "…", URLs never dropped) · `/api/upload` (Blob handleUpload when token + JSON, multipart putFile fallback otherwise — refs/ prefix, jpg/png/webp ≤5MB ≤5 files, per-IP rate limit) · `lib/upload-client.ts` uploadReferenceImages (client validation, browser-image-compression 1.5MB/2000px, blob-client path w/ automatic multipart fallback) · `actions/order.ts` submitProductOrder + submitCustomOrder (public: zod (productId z.cuid()), honeypot+min-fill, rateLimit order:ip 6/10min, REQUIRED customization fields enforced server-side, Inquiry created with whatsappMessage, returns wa.me URL) · ProductOrderPanel (`components/product/order-panel.tsx`, exports OrderPanelProduct type; per-type field rendering SELECT/SWATCH/SIZE/TEXT/NUMBER/FILE, uploader with previews, LIVE `<pre>` summary via buildOrderMessage, Place Order → upload → action → track place_order_clicked + whatsapp_redirected → window.open(wa.me) + router.push(/whatsapp-order?i=id)) · /whatsapp-order fallback (24h inquiry window, message + Open WhatsApp + copy, noindex) · /custom-order (design idea, material/budget/timeline/occasion selects, uploader, live summary, track custom_order_submitted) · track("contact_submitted") added to contact form.
- **Search** page (/search, dynamic): products+posts+portfolio ILIKE, grouped results.
- **Verified E2E** (temp non-DEMO product; DEMO stays excluded from public queries): filters narrow correctly, chips render, PDP full stack, live preview mirrors selections/customer, Place Order → **wa.me/917096036250?text=Hello%20ResinRiva… captured**, Inquiry row w/ reference url saved, fallback page shows summary, custom order fires wa.me. NOTE: hand-inserted test products MUST use cuid-shaped ids (z.cuid() rejects otherwise); happy-dom added for @tiptap/html server build.
### ✅ Phase 8 — Portfolio + Blog (complete; 7/7 checks; search shipped in Phase 7, Instagram strip in Phase 6)
- **Portfolio**: /portfolio (category chip filters from _count.portfolios, PortfolioCard shared server component) + /portfolio/[slug] (preview=1 drafts, ADM meta dl Type/Material/Size/Timeline w/ gold-rule, story, BeforeAfter slider — pointer-captured drag + keyboard role=slider ±5 Home/End, clipPath reveal — only when both images, video band w/ poster, LightboxGallery CSS-columns masonry w/ dialog lightbox, related same-category-first, commission CTA).
- **Blog**: /blog (featured-first layout, category+tag chips, 12/page pagination preserving filters, read-time via module-level textFromTiptap walker) + /blog/[slug] (cache()-deduped fetch for metadata+page, renderTiptapToHtml with legal prose classes + blockquote gold border, tags → filter links, ShareButtons reused (emits share_product event name), related, dopamine CTA). Article JSON-LD is a Phase 10 TODO.
- Convention note: dark hero bands use inline porcelain-ink breadcrumbs (the shared Breadcrumbs component is midnight-ink for light bands).
### ✅ Phase 9 — Motion + Immersive 3D Polish (complete; 8/8 checks)
- **WebGL hero — REMOVED from the homepage per owner request** (the spinning liquid-resin sphere/"black ball"). `HomeHero` no longer mounts `HeroCanvas`; the hero now uses the photographic ocean-resin backdrop (`SITE_IMAGES.hero`) + GradientMesh. The three.js component files below are retained (unused) so the scene can be reinstated. Original scene: `components/three/resin-scene.tsx` (R3F Canvas dpr [1,1.75], icosahedron + MeshTransmissionMaterial sapphire/electric attenuation, drei Float, delta-clamped spin, window-pointermove lean (wrapper is pointer-events-none), scrollProgress ref → scale/drift, procedural Environment w/ Lightformers — ZERO network assets, Bloom 0.9 mipmapBlur) mounted via `hero-canvas.tsx` gatekeeper: gates = reduced-motion, saveData, hardwareConcurrency<4, WebGL2 probe; requestIdleCallback mount; IntersectionObserver ±300px mount/unmount; dynamic ssr:false at module level. HomeHero copy/CTAs stay server-rendered (LCP safe); GradientMesh remains as poster fallback. NOTE: importing @react-three/fiber augments global JSX — LiquidGlass's ElementType tag needed a pinned internal signature (done).
- **Scroll storytelling**: ScrollScene (data-scene-layer 1/2/3 parallax scrub, gsap.context, reduced-motion plain) on the Home brand-story; PinnedStory (sticky+300svh — deliberately NOT ScrollTrigger.pin, Lenis-safe; quickTo crossfades, gold progress rail, outlined numerals) with 3 craft panels on About (section overflow-clip NOT overflow-hidden — hidden creates a scroll container that kills sticky).
- **Micro polish**: CursorGlow (azure radial, mix-blend screen, rAF ref writes, off on touch/reduced-motion) in (public) layout; template.tsx page fade/rise; Button default-variant shine sweep (::after, motion-reduce hidden); input/textarea electric focus halo; footer gradient underline reveals; nav letter-spacing hover; sapphire thin scrollbars + scrollbar-gutter stable.
- **Verified (8/8)**: canvas mounts (even on swiftshader), headline server-rendered, zero page errors, cursor glow live, scene layers tagged, reduced-motion = NO canvas + complete hero, About pinned panels render. Swiftshader renders the form darker than real GPUs (transmission needs hardware) — expected.
### ✅ Phase 10 — SEO + Deploy (complete; SEO verified, deploy owner-run)
- `sitemap.ts` (static routes + PUBLISHED/non-DEMO products/categories/posts/portfolio, lastModified), `robots.ts` (disallow /studio /api /whatsapp-order), `manifest.ts` (theme_color #0F52BA, icon set). `components/seo/json-ld.tsx` sanitized emitter.
- JSON-LD: Organization+LocalBusiness in (public)/layout; Product (AggregateOffer INR, only when showPrice && priceMin) + BreadcrumbList on product; Article + BreadcrumbList on blog post; BreadcrumbList on portfolio; FAQPage on /faq.
- OG images via next/og: default `opengraph-image.tsx` (blue template) + per-product + per-post (runtime nodejs — Prisma). Canonicals on all indexable routes.
- Favicons: `src/app/icon.svg` (RR monogram, sapphire gradient + gold glint) + generated `public/icon-512.png` + `src/app/apple-icon.png`; scaffold favicon.ico removed.
- **DEMO cleanup DONE**: seed.ts no longer creates DEMO products; DB purged (0 products, 0 testimonials — catalog is 100% owner-fed). All 7 docs completed to launch state.
- Verified: sitemap 29 URLs, robots correct, manifest #0F52BA, OG 200 image/png, Organization+LocalBusiness+FAQPage JSON-LD present.
- **Deploy is owner-run** (no Vercel CLI creds here): import repo in Vercel → Neon + Blob storage → env vars → `prisma migrate deploy` + `db:seed` + `db:seed:blogs` → domain www.rivyalivingart.com → enable Analytics/Speed Insights. Full steps in DEPLOYMENT.md. Vercel MCP `deploy_to_vercel` returns CLI instructions only (needs a linked .vercel dir).

### ✅ Phase 11 — Competitor Research + Blog + Catalog Channels (complete — PROJECT DONE)
- **COMPETITOR.md**: 17 evidence-based entries (11 India direct incl. the verified seed list, 4 global, 2 marketplace-lens) in the mandated 7-field template with honest search-snippet hedges; 39 deduped scrape-source candidates (19 RESIN_GOODS / 11 SUPPLIES / 9 PRINT3D) cross-checked against the 34 seeded sources — **added only after live fingerprint verification in production** (egress policy blocked probes here); market-gap + India-pricing + blog-opportunity synthesis.
- **55 original blog posts** in `prisma/blog-content/*.md` (frontmatter + markdown), seeded via `prisma/seed-blogs.ts` (`npm run db:seed:blogs`) → markdownToTiptap (**must be awaited — it's async; forgetting await stores `{}`**), staggered publishedAt Sept 2025→Jul 2026, 195 tags, 6 categories (Gift Guides 17, 3D Printing 10, Weddings & Preservation 10, Trends & Inspiration 7, Care & Maintenance 6, Behind the Studio 5). All original, internal links to real category/custom-order paths, WhatsApp CTA per post. Verified in browser: full body + headings + internal links + Article schema render.
- **Per-category customization templates** (`src/lib/category-templates.ts`) wired into the product form as "Apply category template" (verified: Varmala → 5 fields). Templates for varmala, wedding frames, trays, clocks, jewelry, candles, furniture, workshops + generic fallback. Price-band filters already live in shop (`lib/shop-filters.ts` PRICE_BANDS).
- **Catalog remains EMPTY (owner-fed only)** — zero auto-generated products, per the hard rule. `npm run db:seed:blogs` is the ONLY content seed beyond structure.

## 3. DATABASE SCHEMA (verbatim — implement in Phase 3)

- **User** (id, name, email, hashedPassword, role: ADMIN|EDITOR, createdAt, resetTokenHash?, resetTokenExpiry?)
- **Category** (id, name, slug, image, description, order)
- **Product** (id, title, slug, shortTagline, description, priceMin?, priceMax?, showPrice bool, timeline, materials, dimensions, occasions Json (string array), careNotes? Text, status: DRAFT|PUBLISHED, featured, videoUrl?, model3dUrl?, categoryId, seoTitle, seoDescription, ogImage, needsRewrite Boolean @default(false) (set by the Product Scraper — publish blocked while true), importSource?, importRef? (@@unique([importSource, importRef]) — scraper dedup keys; re-import updates instead of duplicating), createdAt, updatedAt)
- **ProductImage** (id, url, alt, order, productId)
- **CustomizationField** (id, productId, label, type: SELECT|TEXT|SWATCH|SIZE|NUMBER|FILE, options Json, required, helpText, order)
- **Inquiry** (id, source: PRODUCT|CUSTOM_ORDER|CONTACT, productId?, customerName, phone, email?, selections Json, referenceImageUrls Json, budgetRange?, timeline?, notes, whatsappMessage Text, status: NEW|CONTACTED|CONFIRMED|DELIVERED, createdAt)
- **Portfolio** (id, title, slug, story, beforeImageUrl?, afterImageUrl?, videoUrl?, resultsMeta Json, categoryId?, status, createdAt) + **PortfolioImage** (id, url, alt, order, portfolioId)
- **BlogPost** (id, title, slug, excerpt, content Json (Tiptap), coverImage, authorName, blogCategoryId, status, publishedAt, seoTitle, seoDescription) + **BlogCategory** + **Tag** (+ join table)
- **Testimonial** (id, name, location, quote, rating 1–5, avatarUrl?, order)
- **Faq** (id, question, answer, order) — seed with: delivery across India & timelines, packaging safety, custom photo quality requirements, resin care & cleaning, price/payment on WhatsApp, returns for made-to-order items
- **Media** (id, url, pathname, type: IMAGE|VIDEO|DOCUMENT|MODEL3D, folder, bytes, width?, height?, createdAt) — `pathname` = Vercel Blob pathname for delete/list
- **Page** (id, slug unique, title, content Json (Tiptap), seoTitle, seoDescription, updatedAt) — powers Privacy Policy, Terms & Conditions and any future info pages; editable in /studio and bulk-importable
- **SiteSettings** (singleton: brandName, tagline, logoUrl, heroVideoUrl, announcement, phone, whatsappNumber, email, mapsUrl, address, socials Json, defaultSeo Json, defaultCareNotes Text)
- **ActivityLog** (id, userId, action, entity, entityId, createdAt)
- **ScrapeSource** (id, key unique, name, baseUrl, tier: OWNER|RESIN_GOODS|SUPPLIES|PRINT3D, vertical, country, platform: SHOPIFY|WOOCOMMERCE|JSONLD|UNKNOWN, supply bool, enabled bool, verifiedAt?, notes?) — the tiered registry, fully managed in /studio
- **ScrapeJob** (id, sourceId?, inputUrl, sourceKey, sourceName, platform: SHOPIFY|WOOCOMMERCE|JSONLD, vertical, status: QUEUED|RUNNING|DONE|FAILED, cursorPage, totalScraped, newCount, updatedCount, sheetSynced bool, error?, createdAt, finishedAt?)
- **ScrapedProduct** (id, jobId, sourceKey, externalId, url, vertical, currency, title, slug, category?, shortTagline?, description?, priceMin?, priceMax?, showPrice?, timeline?, materials?, dimensions?, status?, featured?, images Json, imageAlts Json, fields Json, seoTitle?, seoDescription?, contentHash, firstSeen, lastSeen, reviewStatus: PENDING|APPROVED|REJECTED|IMPORTED, importedProductId?) — @@unique([sourceKey, externalId])

## 4. DESIGN SYSTEM TOKENS (verbatim — implement in Phase 2 Tailwind theme, keep IDENTICAL across ALL sessions)

Colors:
- `sapphire` **#0F52BA** — **PRIMARY**: all CTAs, buttons, links, active states, focus rings, selection, key headings accents
- `sapphire-deep` #0C3F92 — primary hover/pressed state
- `azure` #3B82F6 — gradients, glows, cursor light, hover accents
- `midnight` **#0A1A2F** — dark navy base: dark sections, footer, primary body text on light (NOT pure black — the darks stay blue)
- `deep-ocean` #0E3A53 — gradient depth layer, hero mesh, resin-imagery echo
- `void` **#05080F** — immersive near-true-black (blue undertone) for WebGL heroes and dark-mode-evolution sections
- `electric` #2E7CF6 — neon-blue GLOW color: used ONLY as light effects (glows, bloom, gradient lines, focus halos) — never as flat text or fill color
- `porcelain` **#F7F7F4** — light base background
- `ice` #E3EEF9 — soft blue tint backgrounds, cards, glass surfaces, section alternation
- `gold` #D4AF37 — the ONLY metallic; used sparingly (thin rules, icon strokes, hover underlines, premium badges); never neon #FFD700
- Glass tints: `rgba(15,82,186,0.08–0.14)` blue-tinted glassmorphism panels with `rgba(255,255,255,0.15)` borders
- Rules: blue leads every screen; gold is garnish; neon appears ONLY as blue light/glow effects (electric), never as flat neon UI colors; NO bright red, NO rainbow dopamine palettes, NO purple, NO baby pastels. Max 4 colors visible per screen.

Semantic mapping: primary=sapphire, primary-hover=sapphire-deep, surface=porcelain, surface-alt=ice, surface-dark=midnight, accent=gold, text=midnight on light / porcelain on dark.

Contrast & accessibility: sapphire on porcelain and white on sapphire both pass WCAG AA — use midnight for long body text; never place gold text on porcelain (decorative only); visible sapphire focus ring on ALL interactive elements.

Section rhythm: alternate porcelain → ice → midnight/void (dark immersive) → porcelain down every page; the hero uses void with the WebGL scene + neon-blue glows, and one storytelling band per page uses midnight with animated blue gradient mesh.

Gradient system: hero/CTA meshes blend void → midnight → deep-ocean → sapphire → azure → electric with slow animated movement (the dopamine-blue energy layer); gold appears only as a 1px divider or particle glint.

Typography (via next/font, free Google Fonts):
- Display/headings: **Fraunces** (or Cormorant Garamond) — elegant serif, large and airy
- Body/UI: **Inter** (or Manrope) — clean sans
- Lowercase eyebrow labels in sapphire + numbered section markers (01, 02, 03…) in gold
- KINETIC TYPE SYSTEM: oversized display scale up to clamp(3rem, 9vw, 11rem); GSAP SplitText per-char/word reveals on all major headings; masked line-by-line paragraph reveals; scroll-velocity skew on marquees; hover letter-shift on nav links; giant outlined numerals as anti-grid accents; animated counters for stats

Currency & locale: prices in INR with Indian digit grouping via `Intl.NumberFormat('en-IN')` → ₹1,50,000. Dates in en-IN. Language: English.

## 5. BRAND RULES (hard rules — never violate)

- NO payment gateway / online checkout / cart payment / Stripe / Razorpay / PayPal
- NO membership / customer login / customer accounts (admin login at /studio is owner-only, not a customer feature)
- NO AI-invented products, EVER — catalog only via: scraper approvals, owner Bulk Import, manual /studio adds. Sole spec-sanctioned exception: ≤3 obviously-fake DEMO-flagged products during the build for UI work only — MUST be deleted in the Phase 10 pre-launch cleanup
- All orders finalized through WhatsApp (save Inquiry to DB first, then redirect to wa.me)
- Scraped content is research reference ONLY: scraper imports are DRAFT + needsRewrite=true; publish blocked until the owner confirms rewrite
- Never copy competitor text, images, or product names — category concepts and structure only
- Exact business values everywhere: WhatsApp `wa.me/917096036250`, phone +91 7096036250, email gondaliyabhavya70960@gmail.com, site https://www.rivyalivingart.com, maps https://maps.app.goo.gl/L2NHDt9Akgqs2ZoT6

## 6. FOLDER STRUCTURE (target — created in Phase 2+)

```
/
├─ README.md, INSTALL.md, DEPLOYMENT.md, ADMIN_GUIDE.md,
│  CONTENT_GUIDE.md, SEO_GUIDE.md, BACKUP_GUIDE.md,
│  WHATSAPP_ORDER_GUIDE.md, COMPETITOR.md, CONTEXT.md
├─ prisma/ (schema.prisma, seed.ts, migrations/)
├─ src/app/
│  ├─ (public)/  → page.tsx (home), about/, shop/, shop/[category]/,
│  │   product/[slug]/, custom-order/, workshops/, portfolio/, portfolio/[slug]/,
│  │   process/, blog/, blog/[slug]/, faq/, contact/, privacy/, terms/,
│  │   search/, whatsapp-order/, not-found.tsx, error.tsx, loading.tsx
│  ├─ studio/    → login/, dashboard, products/, categories/, portfolio/,
│  │   blog/, media/, inquiries/, testimonials/, faqs/, seo/, settings/,
│  │   users/, activity/, import/, scraper/
│  ├─ api/       → auth/, upload/ (Blob handleUpload), search/, og/
│  ├─ layout.tsx, sitemap.ts, robots.ts, manifest.ts
├─ src/components/ (ui/, motion/, layout/, product/, sections/, studio/)
├─ src/lib/ (db.ts, auth.ts, blob.ts, whatsapp.ts, rate-limit.ts, utils.ts, validations/)
├─ src/hooks/  ├─ src/actions/  ├─ src/types/
├─ public/ (logos, fonts, og, video posters)
├─ styles/
```

## 7. API ENDPOINTS & SERVER ACTIONS (inventory — grows per phase)

_None built yet. Planned:_
- `app/api/auth/[...nextauth]` — Auth.js v5 (Phase 3)
- `app/api/upload/route.ts` — Vercel Blob client-upload `handleUpload` (Phase 4/7)
- `app/api/search/route.ts` — public search (Phase 8)
- `app/api/og/route.tsx` — dynamic OG images (Phase 10)
- Server Actions (in `src/actions/`): inquiries (save-then-WhatsApp), products CRUD, categories, blog, portfolio, testimonials, faqs, pages, settings, media, bulk-import, bulk-actions, scraper (jobs, review, approve/import), users, activity. Public actions are Zod-validated + honeypot + min-fill-time + per-IP rate limit.

## 8. COMPONENT INVENTORY (with props — grows per phase)

**Layout (`src/components/layout/`):**
- `Container({ className?, ...div })` — max-w-7xl (1280px) wrapper, widened to `lg:max-w-[1460px]` (+180px) on large screens; px-5/8/12.
- `Section({ headerTheme (REQUIRED, kept for API compat but now ignored — see dark theme), tone?: "porcelain"|"ice"|"midnight"|"deep-ocean"|"void"|"transparent" = porcelain, className?, ...section })` — page band; every top-level public section MUST use it. On the dark site the tones are all dark surfaces (porcelain→bg-background rich black, ice→bg-card lifted panel, midnight/deep-ocean/void→sapphire-navy/black) and it always emits `data-header-theme="dark"` so the header uses light ink.
- `Logo({ className?, href? = "/" })` — the real **ResinRiva script wordmark** (inline SVG from `src/ResinRivaLogo.svg`, `fill="currentColor"`), so it recolors from the in-scope text color. In the public header it follows the section-aware ink: white over dark sections (`text-header-ink`), and the unlayered `[data-ink="dark"] .brand-logo { color: var(--sapphire) }` rule in globals.css swaps it to sapphire over light sections; on dark surfaces (footer, studio, auth) callers pass `text-porcelain`. `LogoWordmark({ className? })` is the bare SVG (height fills parent); `LogoMark({ className? })` — RR monogram SVG. Favicon = `src/app/icon.svg` (blue-gradient RR, Next file convention). `src/ResinRivaFavicon.svg` is the circular emblem asset (currently unused).
- `DynamicHeader()` — fixed z-50 stack (AnnouncementBar + nav row). ONE IntersectionObserver over `[data-header-theme]` with rootMargin `-headerH 0 -(vh-headerH-1) 0`; rAF + 80ms hysteresis; ink via CSS vars `--header-ink(-soft)` + `data-ink` attr; transparent <24px then dual crossfading gradient-glass variants (midnight-blue glass for light ink, porcelain-ice glass for dark ink); MutationObserver re-arms on late sections; mobile overlay `#mobile-menu` glass-dark light-ink always, focus-managed, Escape/route-change close.
- `AnnouncementBar({ message?, href?, className? })` — h-9 strip, inherits header ink, no own bg.
- `Footer()` — midnight, 4-col (brand+socials / explore / studio / contact w/ tel-mailto-maps-wa), gold-rule, legal bar. `data-header-theme="dark"`. Instagram href="#" TODO real handle.
- `FloatingWhatsApp()` — fixed bottom-right z-40, sapphire→#25D366 hover, inline WA glyph, ping ring (stops ~6s, reduced-motion gated), wa.me deep link.

**Motion (`src/components/motion/`):**
- `LiquidGlass({ as? = "div", tone?: "light"|"dark" = light, highlight? = true, className?, children })` — glass/glass-dark rounded-2xl panel (server component).
- `GradientMesh({ className?, intensity?: "subtle"|"bold" = subtle, animated? = true, showGoldGlint? = false })` — absolute aria-hidden blue mesh blobs, CSS-animated (server component).
- `Preloader()` — once per session (sessionStorage `rr-preloader-shown`), skips under reduced motion, ≤2s hard cap, scroll-locked while visible.
- `MouseParallax({ children, className? })` + `MouseParallaxLayer({ depth? = 1, invert?, className?, children })` — context-shared springed pointer tracking, off on touch/reduced-motion.
- `ScrollReveal({ children, className?, delay?, y? = 28, once? = true, blur? = true })` — whileInView fade/rise; plain div under reduced motion.
- `Magnetic({ children, className?, strength? = 0.3, radius? = 120 })` — magnetic pull wrapper, springs back, off on touch/reduced-motion.
- `KineticHeading({ as? = "h2", children, className?, split?: "chars"|"words"|"lines" = words, stagger? = 0.045, delay?, once? = true, scrollTrigger? = true })` — SplitText reveal, waits document.fonts.ready, autoSplit resize handling, static under reduced motion / non-string children.
- `VelocityMarquee({ children, className?, baseVelocity? = 60, repeat? = 4, separator? })` — scroll-velocity-reactive infinite marquee w/ skew, pauses off-screen, static row under reduced motion.

**Providers:** `SmoothScrollProvider({ children })` — Lenis on gsap.ticker (single rAF), ScrollTrigger.update on scroll, kills triggers on route change, disabled under reduced motion.

**UI (`src/components/ui/` — shadcn new-york style, Tailwind v4, data-slot, React 19):** Button (variants default/secondary/outline/ghost/link/destructive; sizes sm/default/lg/xl/icon; asChild; rounded-full pill) · Input · Textarea · Label · Badge (default/secondary/gold/outline) · Separator · Dialog set · Sheet (4 sides) · Accordion set · Select set · Checkbox · DropdownMenu set.

**Hooks:** `usePrefersReducedMotion()` (useSyncExternalStore, SSR=true) · `useIsTouch()` (SSR=false).
**Lib:** `cn()` (extendTailwindMerge — custom classGroups REQUIRED, see §10) · `formatINR`, `formatPriceBand` · `SITE`/`NAV_LINKS`/`FOOTER_LINKS` constants · `buildWaLink(message?)`, `defaultWaGreeting()` · gsap/ScrollTrigger/SplitText registration.

## 9. CURRENT PROGRESS

- **Phases 1–2 COMPLETE** — docs + full scaffold/design system/component library committed and pushed to `claude/resinriva-ecommerce-build-qwf75k`. Build, typecheck, lint, and a 17/17 browser verification suite all pass.
- **ALL 12 BUILD PHASES COMPLETE (1, 2, 3, 4, 5, 5B, 6, 7, 8, 9, 10, 11).** The full application is built, verified in-browser at every phase, and pushed to `claude/resinriva-ecommerce-build-qwf75k`. The catalog is intentionally empty (owner-fed). Remaining owner steps: production deploy (Vercel import + Neon/Blob + domain, per DEPLOYMENT.md), replace placeholder blog cover images, set the real Instagram handle in Site Settings, and (optional) run the Product Scraper against the live tier-1 sources.

## 10. KNOWN ISSUES / OWNER TODO BEFORE LAUNCH

- Remote GitHub repo was empty before Phase 1, so `claude/resinriva-ecommerce-build-qwf75k` became the repository's default (and only) branch — a PR cannot be opened until a second branch exists. If the owner wants `main` as default: create `main` from this branch on GitHub, set it default, then future phases can PR from the working branch.
- **`vercel link` deferred**: no Vercel CLI credentials exist in the remote build environment (login flow is interactive). The owner's Vercel team is `team_YiI01xPJuCLVFK02UcV7KH7h` ("viperworld150326-6041's projects") with ZERO projects — the project gets created when the owner imports the repo in the Vercel dashboard (needed for Phase 3 Neon/Blob anyway). Phase 10 deploy can also use the Vercel MCP `deploy_to_vercel` tool available in these sessions.
- **shadcn registry (`ui.shadcn.com`) is blocked** by this environment's egress policy (403 on CONNECT) — ui components are authored manually in-repo (components.json kept for future CLI compat). Do NOT rely on `npx shadcn add` in these sessions; write new ui components by hand in the same style.
- **lucide-react 1.x has no brand icons** (Instagram/WhatsApp) — use inline SVG glyphs (see footer.tsx, floating-whatsapp.tsx).
- **`cn()` gotchas (build-breaking, subtle)**: tailwind-merge MUST be taught custom utilities via extendTailwindMerge (done in `src/lib/utils.ts` — font-size: text-display/-sm; font-family: font-display/sans; shadow: luxe*/glow). Unregistered custom utilities silently get dropped when merged with same-prefix classes.
- **Tailwind v4 layering**: any hand-written base CSS must sit inside `@layer base` — unlayered rules beat ALL utilities and silently override them.
- **Lightning CSS prefix dedupe**: never hand-write `-webkit-backdrop-filter` next to `backdrop-filter` — the minifier keeps only the last of the pair; write the standard property only and let it auto-prefix.
- **Server restarts in dev sessions**: kill Next servers with `pgrep -f 'next-serve[r]' | xargs -r kill` (bracket avoids self-match) and verify the port actually freed before rebuilding — rebuilding `.next` under a live server serves broken chunks (500s) and poisons browser tests.
- **Favicon set DONE** (Phase 10: icon.svg + 512 PNG + apple-icon) and **brand logo DONE** (real ResinRiva script wordmark SVG, theme-adaptive: white on dark / sapphire on light). Still pending for the owner: real Instagram handle (Site Settings → socials; footer + home Instagram strip wire off it), and real product/blog/portfolio photography (blog covers are placeholders).
- **Blog content re-seed**: `npm run db:seed:blogs` reads `prisma/blog-content/*.md`. It is idempotent (upsert by slug) and safe to re-run. Base structure seed is `npm run db:seed` (admin, 16 categories, FAQs, settings, legal pages — NO products).
- **markdownToTiptap is async** — always `await` it (seed-blogs.ts bug that stored `{}` was this; now fixed). Same applies anywhere Markdown→Tiptap is converted server-side.

## 11. ENVIRONMENT VARIABLES (names only — see .env.example)

```
DATABASE_URL=                # AUTO-INJECTED by Neon native integration; pull locally with `vercel env pull`
AUTH_SECRET=
AUTH_URL=https://www.rivyalivingart.com
ADMIN_EMAIL=gondaliyabhavya70960@gmail.com
ADMIN_PASSWORD=              # used by seed script only
BLOB_READ_WRITE_TOKEN=       # AUTO-INJECTED when Vercel Blob store is connected
RESEND_API_KEY=              # optional — Resend via Vercel Marketplace
GOOGLE_SERVICE_ACCOUNT_JSON= # optional — enables direct Google Sheet sync from the Product Scraper
SCRAPE_SHEET_ID=             # optional — the designated Google Sheet for scraped products
NEXT_PUBLIC_WHATSAPP_NUMBER=917096036250
NEXT_PUBLIC_SITE_URL=https://www.rivyalivingart.com
```

## 12. PROJECT STATUS — COMPLETE

**All 12 build phases are done and pushed.** There is no "next phase." The remaining work is the owner's, not a build phase:

1. **Deploy** (DEPLOYMENT.md): import the repo in Vercel → create Neon Postgres + Blob store in the Storage tab → set env vars (AUTH_SECRET, AUTH_URL, ADMIN_EMAIL/PASSWORD, NEXT_PUBLIC_*) → `prisma migrate deploy` → `npm run db:seed` then `npm run db:seed:blogs` → add domain www.rivyalivingart.com → enable Web Analytics + Speed Insights.
2. **Fill the catalog** — owner-only, three channels: Product Scraper (Studio → Scraper; run the tier-1 sites live, review, approve to DRAFT, rewrite, publish), Bulk Import (Google Sheet/CSV), or manual adds. NO products are auto-generated, ever.
3. **Assets** — replace placeholder blog cover images with real photos, set the real Instagram handle in Site Settings. (Brand logo SVG + favicon are now in place.)

To resume work in a fresh session: read this file, `service postgresql start`, `npm run dev`. Local dev DB + `.env` are gitignored; the schema/migrations/seed regenerate everything.

### Verification ledger (browser-tested per phase)
P2 17/17 · P3 6/6 · P4 11/11 · P5 13/13 · P5B full scraper pipeline (mock store) · P6 16/16 · P7 17/17 E2E · P8 7/7 · P9 8/8 · P10 SEO endpoints + schema · P11 blog render + template button. Every phase: build + lint + typecheck green before commit.
